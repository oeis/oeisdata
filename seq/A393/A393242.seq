%I A393242 #10 Feb 24 2026 15:41:24
%S A393242 0,1,1,0,2,0,4,0,8,1,6,0,30,0,5,6,54,0,48,0,76,6,7,0,470,3,8,30,109,0,
%T A393242 197,0,1151,10,10,9,1232,0,11,14,1060,0,346,0,179,131,13,0,15117,6,
%U A393242 156,16,305,0,1710,15,2516,16,15,0,7367,0,17,212,44498,15,697
%N A393242 a(n) is the number of divisors of n! with exactly n divisors.
%C A393242 a(p) = 0 for every odd prime p. Proof: Since, according to the divisor function, tau(k) = product (e_i + 1) = p, we must have k = q^(p - 1) for some prime q. Thus we would need a prime q whose exponent in the factorization of p! equals p - 1. The largest exponent occurs for q = 2. By Legendre's formula v_2(p!) = p - s_2(p), where v_2(p!) is the exponent of 2 in the prime factorization of p!, and s_2(p) is the sum of the binary digits of p. For any odd prime p, the binary expansion contains at least two 1s, so s_2(p) >= 2. Hence v_2(p!) = p - s_2(p) < p - 1. Therefore no prime can occur with exponent p - 1 in p!, so such a k does not exist and a(p) = 0.
%H A393242 Eric Weisstein's World of Mathematics, <a href="https://mathworld.wolfram.com/DivisorFunction.html">Divisor Function</a>.
%H A393242 Wikipedia, <a href="https://en.wikipedia.org/wiki/Legendre%27s_formula">Legendre's formula</a>.
%F A393242 a(p) = 0 for odd primes p.
%e A393242 The a(6) = 4 divisors of 6! = 720 with exactly 6 divisors are 12 (1, 2, 3, 4, 6, 12), 18 (1, 2, 3, 6, 9, 18), 20 (1, 2, 4, 5, 10, 20) and 45 (1, 3, 5, 9, 15, 45).
%p A393242 A393242 := proc(N)
%p A393242     local a, i, k, l, n, p, t, b, w;
%p A393242     w := proc(f, g)
%p A393242         local c, e, m, r;
%p A393242         if f = 1 then return 1; end if;
%p A393242         if nops(g) = 0 then return 0; end if;
%p A393242         c := 0;
%p A393242         m := g[1];
%p A393242         r := g[2..-1];
%p A393242         for e from 0 to m do
%p A393242             if f mod (e + 1) = 0 then
%p A393242                 c := c + w(f / (e + 1), r);
%p A393242             end if;
%p A393242         end do;
%p A393242         return c;
%p A393242     end proc;
%p A393242     a := [0];
%p A393242     l := [];
%p A393242     t := table();
%p A393242     for n from 1 to N do
%p A393242         p := ifactors(n)[2];
%p A393242         for i in p do
%p A393242             if member(i[1], l) then
%p A393242                 t[i[1]] := t[i[1]] + i[2];
%p A393242             else
%p A393242                 l := [op(l), i[1]];
%p A393242                 t[i[1]] := i[2];
%p A393242             end if;
%p A393242         end do;
%p A393242         b := [seq(t[k], k in l)];
%p A393242         a := [op(a), w(n, b)];
%p A393242     end do;
%p A393242     return a;
%p A393242 end proc:
%p A393242 A393242(66);
%Y A393242 Cf. A000005, A000142, A005179, A027423, A099311, A393241.
%K A393242 nonn,new
%O A393242 0,5
%A A393242 _Felix Huber_, Feb 18 2026
