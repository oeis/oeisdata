%I A324667 #8 Mar 11 2019 20:43:56
%S A324667 0,0,-1,0,-2,-1,0,-1,-2,-1,0,-3,-2,-1,-1,0,-2,-1,-3,-2,-1,0,-1,-3,-2,
%T A324667 -1,-2,-1,0,-6,-1,-1,-3,-2,-1,-1,0,-4,-1,-1,-1,-1,-3,-2,-1,0,-1,-2,-1,
%U A324667 -3,-2,-1,-1,-2,-1,0,-1,-1,-6,-1,-4,-3,-2,-1,-1,-1,0,-3
%N A324667 Starting at n, a(n) is the maximum negative point visited, or zero if no negative points are visited, according to the following rules. On the k-th step (k=1,2,3,...) move a distance of k in the direction of zero. If the number landed on has been landed on before, move a distance of k away from zero instead.
%e A324667 For n=2, the points visited are 2,1,-1,-4,0.  As -1 is the larger of the two negative values, a(2) = -1.
%o A324667 (Python)
%o A324667 #Sequences A324660-A324692 generated by manipulating this trip function
%o A324667 #spots - positions in order with possible repetition
%o A324667 #flee - positions from which we move away from zero with possible repetition
%o A324667 #stuck - positions from which we move to a spot already visited with possible repetition
%o A324667 def trip(n):
%o A324667     stucklist = list()
%o A324667     spotsvisited = [n]
%o A324667     leavingspots = list()
%o A324667     turn = 0
%o A324667     forbidden = {n}
%o A324667     while n != 0:
%o A324667         turn += 1
%o A324667         sign = n // abs(n)
%o A324667         st = sign * turn
%o A324667         if n - st not in forbidden:
%o A324667             n = n - st
%o A324667         else:
%o A324667             leavingspots.append(n)
%o A324667             if n + st in forbidden:
%o A324667                 stucklist.append(n)
%o A324667             n = n + st
%o A324667         spotsvisited.append(n)
%o A324667         forbidden.add(n)
%o A324667     return {'stuck':stucklist, 'spots':spotsvisited,
%o A324667                 'turns':turn, 'flee':leavingspots}
%o A324667 def maxorzero(x):
%o A324667     if x:
%o A324667         return max(x)
%o A324667     return 0
%o A324667 #Actual sequence
%o A324667 def a(n):
%o A324667     d = trip(n)
%o A324667     return maxorzero([i for i in d['spots'] if i < 0])
%Y A324667 Cf. A228474, A324660-A324692.
%K A324667 sign
%O A324667 0,5
%A A324667 _David Nacin_, Mar 10 2019
