%I A324668 #7 Mar 11 2019 20:44:01
%S A324668 0,1,1,2,1,1,3,1,2,1,4,1,1,1,4,5,1,1,3,1,1,6,1,2,1,1,5,1,7,1,1,2,3,1,
%T A324668 1,7,8,1,1,2,3,5,6,1,1,9,1,2,1,4,1,1,7,8,1,10,1,2,3,1,2,1,1,1,9,10,11,
%U A324668 1,1,1,4,5,6,7,1,9,1,1,12,1,2,3,4,5,6,1
%N A324668 Starting at n, a(n) is the minimum positive point visited, or zero if no positive points are visited, according to the following rules. On the k-th step (k=1,2,3,...) move a distance of k in the direction of zero. If the number landed on has been landed on before, move a distance of k away from zero instead.
%e A324668 For n=2, the points visited are 2,1,-1,-4,0.  As 1 is the smaller of the two positive values, a(2) = 1.
%o A324668 (Python)
%o A324668 #Sequences A324660-A324692 generated by manipulating this trip function
%o A324668 #spots - positions in order with possible repetition
%o A324668 #flee - positions from which we move away from zero with possible repetition
%o A324668 #stuck - positions from which we move to a spot already visited with possible repetition
%o A324668 def trip(n):
%o A324668     stucklist = list()
%o A324668     spotsvisited = [n]
%o A324668     leavingspots = list()
%o A324668     turn = 0
%o A324668     forbidden = {n}
%o A324668     while n != 0:
%o A324668         turn += 1
%o A324668         sign = n // abs(n)
%o A324668         st = sign * turn
%o A324668         if n - st not in forbidden:
%o A324668             n = n - st
%o A324668         else:
%o A324668             leavingspots.append(n)
%o A324668             if n + st in forbidden:
%o A324668                 stucklist.append(n)
%o A324668             n = n + st
%o A324668         spotsvisited.append(n)
%o A324668         forbidden.add(n)
%o A324668     return {'stuck':stucklist, 'spots':spotsvisited,
%o A324668                 'turns':turn, 'flee':leavingspots}
%o A324668 def minorzero(x):
%o A324668     if x:
%o A324668         return min(x)
%o A324668     return 0
%o A324668 #Actual sequence
%o A324668 def a(n):
%o A324668     d = trip(n)
%o A324668     return minorzero([i for i in d['spots'] if i > 0])
%Y A324668 Cf. A228474, A324660-A324692.
%K A324668 nonn
%O A324668 0,4
%A A324668 _David Nacin_, Mar 10 2019
