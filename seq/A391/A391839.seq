%I A391839 #7 Dec 23 2025 09:41:17
%S A391839 79,383,3491,17317,207409,4976711,46448719,1088640103,25082265727,
%T A391839 338610585739,6270566400157,147483721728181,2781121609728199,
%U A391839 78219045273600227,2294425328025600257,70501432806604800289,1803968969906847744313,61225613524111196160347,1578472848668491776000373
%N A391839 a(n) is the least prime that can be written as (product + sum) of n distinct composite numbers.
%H A391839 Robert Israel, <a href="/A391839/b391839.txt">Table of n, a(n) for n = 2..429</a>
%e A391839 a(3) = 383 because 383 is prime and 383 = 4 + 9 + 10 + 4 * 9 * 10 where 4, 9 and 10 are three distinct composites, and 383 is the least prime that works.
%p A391839 ithcomp:= proc(n) option remember; local x;
%p A391839   for x from procname(n-1)+1 do
%p A391839     if not isprime(x) then return x fi
%p A391839   od
%p A391839 end proc:
%p A391839 ithcomp(1):= 4:
%p A391839 f:= proc(n) uses priqueue; local L, Lc, Lp, t, i;
%p A391839   initialize(pq);
%p A391839   L:= [seq(i, i=1..n)];
%p A391839   Lc:= map(ithcomp, L);
%p A391839   insert([-convert(Lc, `*`)-convert(Lc, `+`), L], pq);
%p A391839   do
%p A391839     t:= extract(pq);
%p A391839     if isprime(-t[1]) then return -t[1] fi;
%p A391839     L:= t[2];
%p A391839     for i from 1 to n do
%p A391839       if L[i] = L[n]-n+i then
%p A391839         Lp:= L + [0$(i-1), 1$(n-i+1)];
%p A391839         Lc:= map(ithcomp, Lp);
%p A391839         insert([-convert(Lc, `*`)-convert(Lc, `+`), Lp], pq)
%p A391839       fi
%p A391839    od od;
%p A391839 end proc:
%p A391839 A:=map(f, [$2..40]);
%Y A391839 Cf. A391772.
%K A391839 nonn
%O A391839 2,1
%A A391839 _Robert Israel_, Dec 21 2025
