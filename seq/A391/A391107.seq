%I A391107 #26 Dec 06 2025 09:10:56
%S A391107 65,85,130,145,170,185,195,205,221,255,260,265,290,305,325,340,365,
%T A391107 370,377,390,410,425,435,442,445,455,481,485,493,505,510,520,530,533,
%U A391107 545,555,565,580,585,595,610,615,625,629,650,663,680,685,689,697,715,725,730,740,745,754,765,780
%N A391107 Integers k such that the equation x^2 + y^2 = 2*k^2 has at least 4 solutions in positive integers x < y.
%C A391107 These numbers are "square-sum-rich" and represent potential centers for a 3 X 3 magic square of distinct squares. Equivalent to n = (x^2 + y^2)/2 with at least 4 distinct pairs (x<y).
%C A391107 If k = Product_i p_i^e_i, where p_i are distinct primes, then k is a term if and only if Product_{i:p_i mod 4 == 1} 2*e_i+1 >= 9. If k is a term, then k*m is a term as well. A131574 is a subsequence. - _Chai Wah Wu_, Dec 01 2025
%H A391107 Chai Wah Wu, <a href="/A391107/b391107.txt">Table of n, a(n) for n = 1..10000</a>
%o A391107 (Python)
%o A391107 import math
%o A391107 def count_pairs(n):
%o A391107     L = 2 * n**2
%o A391107     count = 0
%o A391107     for x in range(1, int(math.isqrt(L)) + 1):
%o A391107         y2 = L - x*x
%o A391107         y = int(math.isqrt(y2))
%o A391107         if y*y == y2 and y > x:
%o A391107             count += 1
%o A391107     return count
%o A391107 # Generate list of n ≤ MAX with ≥4 pairs
%o A391107 MAX = 1500
%o A391107 vals = [n for n in range(1, MAX+1) if count_pairs(n) >= 4]
%o A391107 print(vals)
%o A391107 (Python)
%o A391107 from math import prod
%o A391107 from itertools import count, islice
%o A391107 from sympy import factorint
%o A391107 def A391107_gen(startvalue=1): # generator of terms >= startvalue
%o A391107     for k in count(max(startvalue,1)):
%o A391107         if prod((e<<1)|1 for p, e in factorint(k).items() if p&3==1)>=9:
%o A391107             yield k
%o A391107 A391107_list = list(islice(A391107_gen(),58)) # _Chai Wah Wu_, Dec 01 2025
%Y A391107 Cf. A084648, A002654, A001481, A131574.
%K A391107 nonn,changed
%O A391107 1,1
%A A391107 _Md. Rad Sarar Anando_, Nov 29 2025
