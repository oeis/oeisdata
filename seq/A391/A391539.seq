%I A391539 #34 Dec 22 2025 14:40:47
%S A391539 2,3,7,17,22,23,27,33,37,38,82,87,97,98,103,107,113,118,122,123,127,
%T A391539 133,137,153,158,387,423,457,477,497,502,503,513,517,518,538,542,543,
%U A391539 553,573,578,583,587,597,602,607,613,617,622,623,627,633,638,647,657,658,662,673,683,687,698
%N A391539 Positive integer multipliers m such that each integer can be written as a sum of progressive powers 5^t multiplied by coefficients in (-m,-1,0,1,m).
%C A391539 Any multiplier provides exactly one representation of each integer.
%C A391539 In order for m>=8 to represent an arbitrary integer it suffices that m represent all integers in the interval [2,m/4].
%C A391539 Any multiplier m satisfies m == 2 (mod 5) or m == 3 (mod 5).
%C A391539 Any multiplier m satisfies m != 0 (mod 4) and m != 0 (mod 6).
%C A391539 The values group into clearly identifiable separate clusters, which concentrate around the powers 5^t.
%C A391539 There is strong evidence (but no proof) that the sequence is unbounded: integers 5^t+2, 5^t-2, 5^t-3, 4*5^t+2, 4*5^t-2 all belong to the sequence for t=1,2,3,4,5,6,7,8.
%C A391539 There is some indication that the inverse values of the multipliers sum up in limit to the golden ratio, (sqrt(5)+1)/2.
%H A391539 Heinrich Puschmann, <a href="/A391539/b391539.txt">Table of n, a(n) for n = 1..8108</a>
%e A391539 For m=7, the first consecutive integers can be written in the form 1=+1*(1), 2=+7*(1)-1*(5), 3=-7*(1)+7*(5)-1*(25), 4=-1*(1)+1*(5), 5=0*(1)+1*(5), ... Since each integer can be written that way, m=7 belongs to the sequence.
%o A391539 (Python)
%o A391539 def no_sirm(mm):
%o A391539    """
%o A391539    returns 0 if every integer is a sum of
%o A391539     progressive powers of 5^t multiplied by coefficients in {-m,-1,0,1,m};
%o A391539     otherwise returns the smallest positive integer not fulfilling the above.
%o A391539    """
%o A391539    assert isinstance(mm,int)
%o A391539    mm = abs(mm)
%o A391539    if mm<2 or mm%5 in (0,1,4): return 2
%o A391539    untested_lst = list(range(2,mm//4+1))
%o A391539    tested_lst = [0,1]
%o A391539    while untested_lst:
%o A391539       testing = rr = untested_lst[0]
%o A391539       rr_lst = []
%o A391539       while rr not in tested_lst:
%o A391539          rr_lst.append(rr)
%o A391539          untested_lst.remove(rr)
%o A391539          if   rr%5==(  0)%5:  rr -= (  0)
%o A391539          elif rr%5==( +1)%5:  rr -= ( +1)
%o A391539          elif rr%5==( -1)%5:  rr -= ( -1)
%o A391539          elif rr%5==(+mm)%5:  rr -= (+mm)
%o A391539          elif rr%5==(-mm)%5:  rr -= (-mm)
%o A391539          rr = abs(rr//5)
%o A391539          if rr in rr_lst: return testing
%o A391539       tested_lst.extend(rr_lst)
%o A391539    return 0
%K A391539 nonn,new
%O A391539 1,1
%A A391539 _Heinrich Puschmann_, Dec 12 2025
