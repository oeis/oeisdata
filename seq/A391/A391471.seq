%I A391471 #56 Feb 16 2026 11:33:26
%S A391471 1,2,3,4,89,179,269,359,449,539,629,719,809,895,896,897,898,899,1348,
%T A391471 1795,1796,1797,1798,1799,2247,2695,2696,2697,2698,2699,3146,3595,
%U A391471 3596,3597,3598,3599,4045,4495,4496,4497,4498,4499,4590,4954,5395,5396,5397,5398,5399,6295,6296,6297,6298,6299
%N A391471 Integers n such that floor((11/10)*n + 1/2) is a digit permutation of n.
%C A391471 Motivation: observed from 10% consumption-tax price rounding.
%C A391471 Leading zeros are not allowed.
%C A391471 If T = floor((11/10)*n + 1/2), then necessary conditions for a k-digit solution are: (i) T < 10^k (no carry to a new digit), (ii) T == n (mod 9), (iii) the multisets of digits of n and T coincide.
%H A391471 Masato Wada, <a href="/A391471/b391471.txt">Table of n, a(n) for n = 1..484</a>.
%F A391471 Let T(n) = floor((11*n + 5)/10). Then n is a term iff length(n) = length(T(n)) and sort(digits(n)) = sort(digits(T(n))).
%e A391471 3598 is a term since T(3598) = floor((11/10)*3598 + 0.5) = 3958, a digit permutation of 3598.
%e A391471 89 is a term since T(89) = 98.
%p A391471 taxround := n -> floor(11*n/10 + 1/2):
%p A391471 isA := proc(n) local t, dn, dt;
%p A391471   t  := taxround(n);
%p A391471   dn := convert(n, base, 10); dt := convert(t, base, 10);
%p A391471   if nops(dn) <> nops(dt) then return false fi;
%p A391471   sort(dn) = sort(dt)
%p A391471 end:
%p A391471 select(isA, [$1..10000]);
%t A391471 T[n_] := Floor[11 n/10 + 1/2];
%t A391471 isA[n_] := With[{t = T[n]},
%t A391471   IntegerLength[n] == IntegerLength[t] &&
%t A391471   Sort[IntegerDigits[n]] === Sort[IntegerDigits[t]]
%t A391471 ];
%t A391471 Select[Range[1, 10000], isA]
%o A391471 (Python)
%o A391471 def T(n): return (11*n + 5)//10
%o A391471 def ok(n):
%o A391471     s, t = str(n), str(T(n))
%o A391471     return len(s) == len(t) and sorted(s) == sorted(t)
%o A391471 print([k for k in range(1, 10**4) if ok(k)])
%o A391471 (Python)
%o A391471 from itertools import count, islice
%o A391471 def A391471_gen(): # generator of terms
%o A391471     return filter(lambda n:sorted(str(n))==sorted(str(11*n+5)[:-1]),count(1))
%o A391471 A391471_list = list(islice(A391471_gen(),54)) # _Chai Wah Wu_, Dec 19 2025
%o A391471 (PARI)
%o A391471 T(n)=(11*n+5)\10;
%o A391471 isA(n)=
%o A391471 {
%o A391471   my(t=T(n));
%o A391471   if(#digits(n)!=#digits(t), return(0));
%o A391471   vecsort(digits(n))==vecsort(digits(t))
%o A391471 };
%o A391471 select(isA, [1..10000])
%K A391471 nonn,base,changed
%O A391471 1,2
%A A391471 _Masato Wada_, Dec 14 2025
