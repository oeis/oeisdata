%I A391431 #31 Jan 05 2026 15:10:19
%S A391431 1,1,3,2,2,7,5,8,3,3,13,10,7,11,15,4,4,21,17,22,9,23,19,24,5,5,31,26,
%T A391431 16,27,11,28,17,29,35,6,6,43,37,44,19,45,13,46,20,47,41,48,7,7,57,50,
%U A391431 29,51,59,52,15,53,61,54,31,55,63,8,8,73,65,74,33,25,67
%N A391431 Numerator of k + a/k where n = k^2 + 2*a + 1 and -k <= a <= k.
%C A391431 One can make one of the conditions -k <= a or a <= k strict inequality if desired to avoid collisions (but the definitions at these endpoints are compatible). The quantity k + a/k is the largest sum of sidelengths of n squares packed into a unit square with all sides parallel to the coordinate axes (Erdős problem 106). The reciprocal 1 / (k+a/k) is also the infimal value of max sum x_{i_r} where x_1,...,x_n are positive distinct reals summing to 1 and the maximum is over monotone subsequences (Erdős problem 1026).
%C A391431 Fixed points are listed in A132411. - _Ivan N. Ianakiev_, Dec 09 2025
%D A391431 C. Campbell and W. Staton, A square-packing problem of Erdős, Am.Math.Mon., 112.2(2005), pp.165-167.
%D A391431 P. Erdős and A. Soifer, Squares in a square, Geombinatorics 4.4(1995), pp. 110-114.
%D A391431 I. Praton, Packing squares in a square, Math.Mag. 81.5 (2008), pp. 358-361.
%H A391431 Jakub Buczak, <a href="/A391431/b391431.txt">Table of n, a(n) for n = 1..10000</a>
%H A391431 Jineon Baek, Junnosuke Koizumi, and Takahiro Ueoro, <a href="https://arxiv.org/abs/2411.07274">A note on the Erdős conjecture about square packing</a>, arXiv:2411.07274 [math.CO], 2024.
%H A391431 Thomas Bloom, <a href="https://www.erdosproblems.com/106">Problem 106</a>, Erdős Problems.
%H A391431 Thomas Bloom, <a href="https://www.erdosproblems.com/1026">Problem 1026</a>, Erdős Problems.
%e A391431 If n=8 then n = 3^2 + 2*(-1) + 1, so k+a/k = 3 + (-1)/3 = 8/3 and so a(8)=8.
%e A391431 If n=9 then one can write n = 2^2 + 2*(2) + 1 or n = 4^2 + 2*(-4) + 1, but in either case k+a/k is equal to 2 + 2/2 = 4 + (-4)/4 = 3/1 and so a(9)=3.
%e A391431 If n=13 then n = 4^2 + 2*(-2) + 1, so k+a/k = 4 + (-2)/4 = 7/2 and so a(13)=7.
%p A391431 f := proc(n) local k, a;
%p A391431   for k from 1 while (k-1)^2 <= n do
%p A391431     a := (n - k^2 - 1)/2;
%p A391431     if type(a, integer) and -k <= a and a <= k then
%p A391431       return k + a/k;
%p A391431     fi;
%p A391431   od;
%p A391431 end:
%p A391431 a := n -> numer( f(n) ):
%p A391431 seq(a(n), n=1..80);
%t A391431 f[n_Integer?Positive] := Module[{k, a},
%t A391431   For[k = 1, (k - 1)^2 <= n, k++,
%t A391431     a = (n - k^2 - 1)/2;
%t A391431     If[IntegerQ[a] && -k <= a <= k,
%t A391431       Return[k + a/k];
%t A391431     ];
%t A391431   ];
%t A391431 ];
%t A391431 a[n_] := Numerator[f[n]];
%o A391431 (Python)
%o A391431 from math import gcd
%o A391431 def A391431(n):
%o A391431     for k in range(1,n+2):
%o A391431         a = n-k**2-1
%o A391431         if a&1^1 and abs(b:=a>>1)<=k:
%o A391431             return (k*k+b)//gcd(b,k) # _Chai Wah Wu_, Dec 10 2025
%Y A391431 Cf. A132411, A391490.
%K A391431 nonn,frac
%O A391431 1,3
%A A391431 _Terence Tao_, Dec 09 2025
