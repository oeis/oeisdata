%I A375493 #15 Sep 20 2024 06:20:08
%S A375493 0,0,1,0,-1,1,3,0,2,-1,-4,1,-2,3,0,0,-3,2,7,-1,4,-4,-12,1,-2,-2,3,3,8,
%T A375493 0,0,0,-3,-3,2,2,-6,7,-1,-1,-9,4,17,-4,1,-12,-33,1,6,-2,-2,-2,-10,3,
%U A375493 -5,3,8,8,21,0,0,0,5,0,-3,-3,-3,-3,-11,2,2,2,15,-6
%N A375493 "s" coefficients of a log_2-like sequence of algebraic integers of the form (r+phi*s), where r and s are integers and phi = (1+sqrt(5))/2 is the golden ratio.
%C A375493 This is one of a pair of co-recursive sequences: A375484 and A347493.
%F A375493 f(n) = A375484(n) + phi*A347493(n)
%F A375493 phi = (1+sqrt(5))/2
%F A375493 for integer x > 1, y > 1 and prime p:
%F A375493 f(x*y) = f(x) + f(y)
%F A375493 f(p) = (f(p-1) + phi*f(p+1)) / (1 + phi)
%F A375493 f(2) = 1
%o A375493 (Python)
%o A375493 from sympy import primefactors, isprime
%o A375493 def a(n): # A375484
%o A375493     if n in {1,2}: return n-1
%o A375493     if isprime(n): return 2*a(n-1) + b(n+1) - (a(n+1) + b(n-1))
%o A375493     return (lambda f: a(f) + a(n//f))(primefactors(n)[0])
%o A375493 def b(n): # A347493
%o A375493     if n in {1,2}: return 0
%o A375493     if isprime(n): return b(n-1) + a(n+1) - a(n-1)
%o A375493     return (lambda f: b(f) + b(n//f))(primefactors(n)[0])
%Y A375493 Cf. A001622, A375484.
%K A375493 sign,more,new
%O A375493 1,7
%A A375493 _Michael J Norris_, Aug 17 2024
