%I A350374 #16 Nov 09 2025 22:13:15
%S A350374 420,630,660,780,840,924,990,1020,1050,1092,1140,1170,1320,1380,1386,
%T A350374 1428,1470,1530,1540,1560,1596,1638,1650,1680,1710,1716,1740,1820,
%U A350374 1848,1860,1890,1932,1950,2040,2070,2142,2184,2220,2244,2280,2380,2394,2436,2460,2508,2550
%N A350374 Numbers with exactly 7 semiprime divisors.
%C A350374 Numbers with prime signature {1,1,1,k} with k >= 2. - _Robert Israel_, Nov 09 2025
%H A350374 Robert Israel, <a href="/A350374/b350374.txt">Table of n, a(n) for n = 1..10000</a>
%p A350374 N:= 10^4: # for terms <= N
%p A350374 P:= select(isprime, [2, seq(i, i=3..N/(2^2*3*5), 2)]):
%p A350374 nP:= nops(P):
%p A350374 Res:= NULL:
%p A350374 for i1 from 1 to nP do
%p A350374   p1:= P[i1];
%p A350374   for i2 from i1+1 to nP do
%p A350374     p2:= P[i2];
%p A350374     if p1 * p2 * 3 * 4 > N then break fi;
%p A350374     for i3 from i2 + 1 to nP do
%p A350374       p3:= P[i3];
%p A350374     if p1 * p2 * p3 * 4 > N then break fi;
%p A350374       for i4 from 1 to nP do
%p A350374           if member(i4,{i1,i2,i3}) then next fi;
%p A350374           p4:= P[i4];
%p A350374           for n4 from 2 do
%p A350374             x:= p1 * p2  * p3 * p4^n4;
%p A350374             if x > N then break fi;
%p A350374             Res:= Res, x
%p A350374 od od od od od:
%p A350374 Res:= sort([Res]); # _Robert Israel_, Nov 09 2025
%t A350374 q[n_] := DivisorSum[n, 1 &, PrimeOmega[#] == 2 &] == 7; Select[Range[2500], q] (* _Amiram Eldar_, Dec 28 2021 *)
%o A350374 (PARI) isok(k) = sumdiv(k, d, bigomega(d)==2) == 7; \\ _Michel Marcus_, Dec 28 2021
%Y A350374 Numbers with exactly k semiprime divisors: A346041 (k=1), A345381 (k=2), A345382 (k=3), A350371 (k=4), A350372 (k=5), A350373 (k=6), this sequence (k=7), A350375 (k=8).
%K A350374 nonn
%O A350374 1,1
%A A350374 _Wesley Ivan Hurt_, Dec 27 2021
