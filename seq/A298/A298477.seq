%I A298477 #20 Jan 25 2026 03:32:48
%S A298477 2,2,4,12,24,12,28,4,16,60,4,24,140,2,32,230,1112,36,332,4,56,542,
%T A298477 3572,50,712,2,64,958,8696,50
%N A298477 a(n) is the number of length n strings over a two-letter alphabet that have a minimum palindromic partition size of A090701(n).
%C A298477 A "minimum palindromic partition size" of a string is the fewest number of palindromes that the string can be partitioned into.
%C A298477 All terms are even because the letters of the alphabet can be swapped (e.g., "101100" can become "010011".)
%e A298477 The a(6) = 12 six-character strings that require A090701(6) = 3 partitions are:
%e A298477   100101 via (1001)(0)(1),
%e A298477   100110 via (1001)(1)(0),
%e A298477   101001 via (1)(0)(1001),
%e A298477   101100 via (101)(1)(00),
%e A298477   110010 via (1)(1001)(0),
%e A298477   110100 via (11)(010)(0),
%e A298477 along with the six strings made from swapping the 0's and 1's.
%o A298477 (Python)
%o A298477 from functools import cache
%o A298477 from itertools import product
%o A298477 def A090701(n): return n//6 + (n+4)//6 + 1 if n != 11 else 5
%o A298477 @cache
%o A298477 def f(w):
%o A298477     if len(w) < 2: return len(w)
%o A298477     return min(1+f(w[i:]) for i in range(1, len(w)+1) if (t:=w[:i]) == t[::-1])
%o A298477 def a(n):
%o A298477     t = A090701(n) # target value
%o A298477     return 2*sum(1 for w in product("01", repeat=n-1) if f("0"+"".join(w)) == t)
%o A298477 print([a(n) for n in range(1, 19)]) # _Michael S. Branicky_, Jan 24 2026
%Y A298477 Cf. A090701.
%K A298477 nonn,more,changed
%O A298477 1,1
%A A298477 _Peter Kagey_, Jan 19 2018
%E A298477 a(21)-a(30) from _Michael S. Branicky_, Jan 25 2026
