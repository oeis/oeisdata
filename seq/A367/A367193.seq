%I A367193 #19 Nov 18 2023 13:26:44
%S A367193 0,1,2,3,4,4,5,6,7,7,8,9,10,11,11,12,13,14,14,15,16,17,17,18,19,20,20,
%T A367193 21,22,23,23,24,25,26,27,27,28,29,30,30,31,32,33,33,34,35,36,36,37,38,
%U A367193 39,39,40,41,42,42,43,44,45,45,46,47,48,49,49,50,51,52
%N A367193 a(n) is the numerator of the fraction whose numerator + denominator = n that is closest to Pi. The fraction does not need to be in simplest form; the numerator and denominator must be integers.
%C A367193 a(n) is nondecreasing; lim_{n->oo} a(n) = oo.
%C A367193 The program supplied may not be accurate for n > 2^15.
%F A367193 Conjecture: a(n) is either ceiling(n*Pi/(1+Pi)) or floor(n*Pi/(1+Pi)).
%F A367193 a(n) = round((2*n*Pi + n - sqrt(Pi^2 + 2*Pi + n^2 + 1))/(2*Pi + 2)). - _Jon E. Schoenfield_, Nov 17 2023
%e A367193 For n = 3, the possible fractions are 0/3, 1/2, 2/1 (as 3/0 is undefined and any negative fraction would would be farther from Pi than 0/3). Of these, the closest fraction to Pi is 2/1 so a(3) = 2.
%o A367193 (Java)
%o A367193 import java.util.Scanner;
%o A367193 public class A367193
%o A367193 {
%o A367193     public static int getNumerator(int term)
%o A367193     {
%o A367193         double distance = Double.MAX_VALUE;
%o A367193         double denom;
%o A367193         double numer;
%o A367193         int numerator = 0;
%o A367193         for(denom=term; denom>0; denom--)
%o A367193         {
%o A367193             numer=term-denom;
%o A367193             if (distance > Math.abs(Math.PI-numer/denom))
%o A367193             {
%o A367193                 distance = Math.abs(Math.PI-numer/denom);
%o A367193                 numerator = (int) numer;
%o A367193             }
%o A367193         }
%o A367193         return numerator;
%o A367193     }
%o A367193     public static void main(String[] args)
%o A367193     {
%o A367193         int term = 1;
%o A367193         while (true) {
%o A367193             term++;
%o A367193             System.out.print(getNumerator(term)+", ");
%o A367193         }
%o A367193     }
%o A367193 }
%Y A367193 Cf. A367194 (denominator), A000796, A002485.
%K A367193 nonn,frac,new
%O A367193 1,3
%A A367193 _Colin Linzer_, Nov 09 2023
