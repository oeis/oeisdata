%I A389824 #10 Oct 19 2025 09:52:06
%S A389824 1,11,101,1013,13,3,23,2,29,229,1229,12239,1223,223,2203,12203,102203,
%T A389824 10223,102023,1020023,20023,2003,20063,2063,263,2633,233,2333,20333,
%U A389824 203233,20233,120233,1020233,102233,1022033,122033,1220333,220333,2203133,223133
%N A389824 a(1) = 1; for n > 1, a(n) is the smallest unused prime number that can be created by either removing or adding a single digit anywhere in the value of a(n-1).
%C A389824 On removing the leading digit, any subsequent leading zeros are also removed.
%C A389824 The sequence is finite, the final term being a(17608) = 10253452803799, after which all digit removals or additions to 10253452803799 lead to composites or primes that already appear in the sequence.
%C A389824 The largest term is a(9551) = 100000000000206050030346930504857.
%C A389824 The list of missing primes begins 7, 17, 19, 31, 37, 41, 43, 47, 61, ... .
%C A389824 See A389825 for the sequence starting at 2.
%H A389824 Scott R. Shannon, <a href="/A389824/b389824.txt">Table of n, a(n) for n = 1..17608</a>
%e A389824 a(10) = 229 as a(9) = 29, and the primes created from removing a single digit from 29 are 2, which has been used, while the primes created from adding a single digit to 29 are 229, 829, 929, 239, 269, 293. Of those 229 is the smallest and is therefore the next term chosen.
%o A389824 (Python)
%o A389824 from gmpy2 import is_prime
%o A389824 from itertools import islice
%o A389824 def agen(): # generator of terms
%o A389824     an, aset = 1, {1}
%o A389824     while an != -1:
%o A389824         yield an
%o A389824         aset.add(an)
%o A389824         s = str(an)
%o A389824         D = set(p for i in range(len(s)) if len(t:=s[:i]+s[i+1:]) and is_prime(p:=int(t)))
%o A389824         if D and (Dcands:=D-aset):
%o A389824             an = min(D - aset)
%o A389824             continue
%o A389824         A = set(p for i in range(len(s)+1) for d in "0123456789" if is_prime(p:=int(s[:i]+d+s[i:])))
%o A389824         an = min(Acands) if A and (Acands:=A-aset) else -1
%o A389824     print(f"Last term is a({len(aset)}).")
%o A389824 print(list(islice(agen(), 40))) # _Michael S. Branicky_, Oct 19 2025
%Y A389824 Cf. A389825 (start at 2), A000040, A080603, A080608, A068166.
%K A389824 nonn,fini,full,base
%O A389824 1,2
%A A389824 _Scott R. Shannon_, Oct 16 2025
