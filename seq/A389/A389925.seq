%I A389925 #12 Oct 21 2025 13:14:45
%S A389925 1,2,5,3,7,11,19,37,17,41,73,137,277,139,67,131,263,523,1051,2099,
%T A389925 1049,521,257,577,1153,641,1283,643,1291,647,1303,2647,599,311,151,23,
%U A389925 43,83,163,331,167,71,199,103
%N A389925 a(1) = 1; for n > 1, a(n) is the smallest unused prime number, when read in binary, that can be created by either removing or adding a single 0 or 1 anywhere in the binary value of a(n-1).
%C A389925 The binary version of A389824.
%C A389925 The sequence is finite, the last term being a(44) = 103, after which all removals or additions of a single 0 or 1 to the binary value of 103 = 1100111_2 lead to composites or primes, when read in binary, that already appear in the sequence.
%e A389925 a(8) = 37 = 100101_2 as a(7) = 19 = 10011_2, and the primes created from removing a single 0 or 1 from 10011_2 are 3 = 11_2 and 11 = 1011_2, both of which have already been used, while the primes created from adding a single 0 or 1 to 10011_2 are 37 = 100101_2 and 43 = 101011_2. Of those 37 is the smallest and is therefore the next term chosen.
%o A389925 (Python)
%o A389925 from gmpy2 import is_prime
%o A389925 from itertools import islice
%o A389925 def agen(): # generator of terms
%o A389925     an, aset = 1, {1}
%o A389925     while an != -1:
%o A389925         yield an
%o A389925         aset.add(an)
%o A389925         b = bin(an)[2:]
%o A389925         D = set(p for i in range(len(b)) if len(t:=b[:i]+b[i+1:]) and is_prime(p:=int(t, 2)))
%o A389925         if D and (Dcands:=D-aset):
%o A389925             an = min(D - aset)
%o A389925             continue
%o A389925         A = set(p for i in range(len(b)+1) for d in "01" if is_prime(p:=int(b[:i]+d+b[i:], 2)))
%o A389925         an = min(Acands) if A and (Acands:=A-aset) else -1
%o A389925     print(f"Last term is a({len(aset)}).")
%o A389925 print(list(islice(agen(), 45))) # _Michael S. Branicky_, Oct 21 2025
%Y A389925 Cf. A389824 (base 10), A389825, A387852, A389926, A000040, A080603, A080608, A068166.
%K A389925 nonn,fini,full,base
%O A389925 1,2
%A A389925 _Scott R. Shannon_, Oct 19 2025
