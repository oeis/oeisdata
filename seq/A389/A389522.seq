%I A389522 #25 Oct 12 2025 15:18:37
%S A389522 0,1,1,2,2,2,1,1,2,1,1,2,2,2,1,1,2,2,2,1,1,2,2,2,2,2,1,1,2,2,2,1,1,2,
%T A389522 1,1,2,2,2,1,1,2,2,2,2,2,1,1,2,1,1,2,2,2,1,1,2,2,2,2,2,1,1,2,2,2,1,1,
%U A389522 2,2,2,1,1,2,1,1,2,2,2,1,1,2
%N A389522 Continued fraction expansion of 3*Sum_{k>=0} 6^(-2^k).
%C A389522 All sums of the form:
%C A389522 C*Sum_{k>=0} (2*C)^-2^k, where C is a constant in N, C>=2, have simple continued fractions that are the blocks of '1, 1' and '2' in A157196, interleaved with C - 1.
%C A389522 In this sequence, the ratio of 1's to 2's approaches 2/3, and therefore the density of 1's approaches 2/5, as a corollary of Shallit's paper.
%H A389522 Daniel Hoyt, <a href="/A389522/b389522.txt">Table of n, a(n) for n = 0..1000</a>
%H A389522 Daniel Hoyt, <a href="https://doi.org/10.5281/zenodo.16990656">Proof of the continued fraction formula.</a>, Zenodo, 2025, pp 5-6.
%H A389522 Jeffrey Shallit, <a href="https://arxiv.org/abs/2501.00784">Cloitre's Self-Generating Sequence</a>, arXiv:2501.00784 [math.CO], 2025.
%F A389522 This sequence is an interleaving of the blocks of '1, 1' and '2' in A157196, and the number 2.
%e A389522 0.58564993427175385477531... = 0 + 1/(1 + 1/(1 + 1/(2 + 1/(2 + ...)))).
%o A389522 (Python)
%o A389522 def a(n):
%o A389522     if n < 0: raise ValueError("n>=0")
%o A389522     if n == 0: return 0
%o A389522     s=[1, 1]; h=2; p=0; idx=0; r=1
%o A389522     while True:
%o A389522         while len(s) < p+2:
%o A389522             t = 2 if (h & 1) == 0 else 1
%o A389522             s += [t]*((s[h-1]*2)//t); h += 1
%o A389522         if s[p]==2:
%o A389522             p+=1; idx+=1
%o A389522             if idx==n: return 2
%o A389522             K=((r & -r).bit_length()-1)+1
%o A389522             idx+=1
%o A389522             if idx==n: return int(2)
%o A389522         elif p+1<len(s) and s[p]==1 and s[p+1]==1:
%o A389522             p+=2; idx+=1
%o A389522             if idx==n: return 1
%o A389522             idx+=1
%o A389522             if idx==n: return 1
%o A389522             K=((r & -r).bit_length()-1)+1
%o A389522             idx+=1
%o A389522             if idx==n: return int(2)
%o A389522         else:
%o A389522             continue
%o A389522         r+=1
%Y A389522 Cf. A006466, A007400, A014577, A157196, A336810, A386384, A388655, A387398.
%Y A389522 Cf. A389527 (decimal expansion).
%K A389522 nonn,cofr
%O A389522 0,4
%A A389522 _Daniel Hoyt_, Oct 07 2025
