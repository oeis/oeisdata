%I A389684 #21 Oct 20 2025 18:35:14
%S A389684 24,50,59,72,87,100,118,130,135,138,163,180,184,188,193,218,236,239,
%T A389684 243,251,268,292,302,307,309,326,338,340,354,362,372,380,385,389,391,
%U A389684 413,442,453,466,471,481,498,503,508,509,533,550,553,558,564,579,588,589,592
%N A389684 Number of representations of n as binomial(i,4) + binomial(j,3) + binomial(k,2) + binomial(l,1) with i,j,k,l>=0.
%C A389684 This is the combinatorial number system of degree t = 4, but without the standard ordering requirement i>j>k>l>=0. Instead, we only require i,j,k,l>=0.
%C A389684 Exactly one of the a(n) representations of n always satisfies i>j>k>l>=0. For example, of the 1062 representations of 100, only {i,j,k,l}={8,6,5,0} is strictly decreasing.
%D A389684 D. E. Knuth, The Art of Computer Programming, vol. 4A, Combinatorial Algorithms, Section 7.2.1.3, Eq. (20), p. 360.
%H A389684 David Dewan, <a href="/A389684/b389684.txt">Table of n, a(n) for n = 0..1000</a>
%H A389684 David Dewan, <a href="/A389684/a389684.pdf">Plot of A389683 and A389684</a>.
%H A389684 Wikipedia, <a href="https://en.wikipedia.org/wiki/Combinatorial_number_system">Combinatorial number system</a>.
%F A389684 G.f.: Sum_{i>=0} z^binomial(i,4) * Sum_{j>=0} z^binomial(j,3) * Sum_{k>=0} z^binomial(k,2) / (1-z). - _Robert Israel_, Oct 13 2025
%e A389684 a(10)=163 because the 163 tuples {i,j,k,l} = {0,0,0,10}, {0,0,1,10}, {0,0,2,9},...{5,4,0,1}, {5,4,1,1}, {5,4,2,0} each represent 10.
%p A389684 N:= 100: # for a(0) .. a(N)
%p A389684 A:= Array(0..N):
%p A389684 for i from 0 do
%p A389684   s1:= binomial(i,4);
%p A389684   if s1 > N then break fi;
%p A389684   for j from 0 do
%p A389684     s2:= binomial(j,3) + s1;
%p A389684     if s2 > N then break fi;
%p A389684     for k from 0 do
%p A389684       s3:= binomial(k,2) + s2;
%p A389684       if s3 > N then break fi;
%p A389684       for l from 0 do
%p A389684         s4:= l + s3;
%p A389684         if s4 > N then break fi;
%p A389684         A[s4]:= A[s4]+1
%p A389684 od od od od:
%p A389684 convert(A,list); # _Robert Israel_, Oct 13 2025
%t A389684 maxN=100;rangeI=Range[0,Ceiling[(24*maxN)^(1/4)]+1];
%t A389684 rangeJ=Range[0,Ceiling[CubeRoot[6*maxN]+1]];
%t A389684 rangeK=Range[0,Ceiling[Sqrt[2*maxN]+1]];
%t A389684 rangeL=Range[0,maxN];allQuads=Tuples[{rangeI,rangeJ,rangeK,rangeL}];
%t A389684 conv[x_List]:=Total[MapThread[Binomial,{x,{4,3,2,1}}]]
%t A389684 assoc=GroupBy[allQuads,conv[#]&];assoc=KeySort[KeySelect[assoc,#<=maxN&]];
%t A389684 valueLengths=Length/@Values[assoc]
%Y A389684 Cf. A389683, A194882, A194883, A194884.
%K A389684 nonn
%O A389684 0,1
%A A389684 _David Dewan_, Oct 10 2025
