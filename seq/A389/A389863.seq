%I A389863 #29 Nov 01 2025 15:34:56
%S A389863 14,15,16,21,22,26,27,30,34,36,38,46,58,62,74,82,86,94,106,118,122,
%T A389863 134,142,146,158,166,178,194,202,206,214,218,226,254,262,274,278,298,
%U A389863 302,314,326,334,346,358,362,382,386,394,398,422,446,454,458,466,478,482
%N A389863 Numbers k with exactly two distinct repeated values among the fractions (2*k mod m)/m for 1 <= m <= k.
%C A389863 Conjecture: All numbers of the form 2*p with p prime and p > 5 belong to the sequence, and no other numbers greater than 36 do.  Empirically verified for k <= 100000.
%H A389863 Michael S. Branicky, <a href="/A389863/b389863.txt">Table of n, a(n) for n = 1..10000</a>
%e A389863 For k = 12, the fractions for 1 <= m <= 12 are 0, 0, 0, 0, 4/5, 0, 3/7, 0, 2/3, 2/5, 2/11, 0. Only 0 is repeated (one alignment), so 12 is not in this sequence.
%e A389863 For k = 14, the fractions for 1 <= m <= 14 are 0, 0, 1/3, 0, 3/5, 2/3, 0, 1/2, 1/9, 4/5, 6/11, 1/3, 2/13, 0. Exactly two values are repeated (0 and 1/3), so 14 is in this sequence.
%p A389863 filter:= proc(k) local m,t,S,R,s;
%p A389863   t:= 0; S:= {}: R:= {}:
%p A389863   for m from 1 to k do
%p A389863     s:= (2*k mod m)/m;
%p A389863     if member(s,S) then
%p A389863       if not member(s,R) then
%p A389863         t:= t+1; if t = 3 then return false fi;
%p A389863         R:= R union {s}
%p A389863       fi
%p A389863     else S:= S union {s}
%p A389863     fi
%p A389863   od;
%p A389863   t = 2
%p A389863 end proc:
%p A389863 select(filter, [$1..600]); # _Robert Israel_, Oct 19 2025
%t A389863 okQ[k_]:=Module[{fr={}},Do[AppendTo[fr,Mod[2k,m]/m],{m,1,k}];Length[Select[Last/@Tally[fr],#>1&]]==2];Select[Range[482],okQ] (* _James C. McMahon_, Nov 01 2025 *)
%o A389863 (Python)
%o A389863 from fractions import Fraction
%o A389863 def ok(k, target=2):
%o A389863     seen, dup = set(), set()
%o A389863     for m in range(1, k + 1):
%o A389863         f = Fraction((2 * k) % m, m)
%o A389863         if f in seen:
%o A389863             dup.add(f)
%o A389863         else:
%o A389863             seen.add(f)
%o A389863     return len(dup) == target
%o A389863 target = 2
%o A389863 for k in range(2, 300):
%o A389863     if ok(k, target):
%o A389863         print(k)
%o A389863 (Python)
%o A389863 from gmpy2 import mpq
%o A389863 from collections import Counter
%o A389863 def ok(n):
%o A389863     C, F = set(), set()
%o A389863     for m in range(1, n+1):
%o A389863         f = mpq(2*n%m, m)
%o A389863         if f in F:
%o A389863             C.add(f)
%o A389863             if len(C) > 2: return False
%o A389863         else:
%o A389863             F.add(f)
%o A389863     return len(C) == 2
%o A389863 print([k for k in range(1, 483) if ok(k)]) # _Michael S. Branicky_, Oct 23 2025
%Y A389863 Cf. A387426, A100484.
%K A389863 nonn
%O A389863 1,1
%A A389863 _Kenneth J Scheller_, Oct 17 2025
