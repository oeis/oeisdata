%I A389367 #48 Nov 12 2025 16:41:29
%S A389367 0,1,2,3,3,4,4,5,4,4,5,6,5,7,6,5,4,8,5,9,6,6,7,10,6,5,8,5,7,11,6,12,5,
%T A389367 7,9,6,6,13,10,8,7,14,7,15,8,6,11,16,6,6,6,9,9,17,6,7,8,10,12,18,7,19,
%U A389367 13,7,5,8,8,20,10,11,7,21,7,22,14,6,11,7,9
%N A389367 Semilengths of the Dyck natural numbers.
%C A389367 The Dyck natural numbers form a subset of the Dyck language in which each word uniquely represents a nonnegative integer.  The representation arises from a recursive generalization of prime factorization defined by the standard RPF natural spelling function gamma_{N_r} (here shortened to simply "gamma" for readability), according to Definition 2.11 in arXiv:2102.02777:
%C A389367 gamma(0) = epsilon, where epsilon denotes the empty string (length = 0).
%C A389367 gamma(1) = "()".
%C A389367 For n > 1, write n as a product of powers of consecutive primes from 2 up to and including the greatest prime factor gpf(n).
%C A389367 Each exponential term prime(k)^m is enclosed in parentheses, and if m > 0 it is replaced recursively by gamma(m).
%C A389367 After all replacements, discard all non-parenthesis symbols; the remaining parentheses form gamma(n).
%C A389367 The sequence term a(n) is half the length (i.e., the semilength) of gamma(n).
%H A389367 Ralph L. Childress, <a href="https://arxiv.org/abs/2102.02777">Recursive Prime Factorizations: Dyck Words as Representations of Numbers</a>, arXiv:2102.02777 [cs.FL], 2021. See especially sections 2.1 and 2.3, as well as Table 1.
%F A389367 a(0) = 0, a(1) = 1, for n > 1, a(n) = Sum_{p <= gpf(n), p prime} (1 + a(val_p(n))), where gpf(n) is the greatest prime factor of n, and val_p(n) (the p-adic valuation) is the largest integer k >= 0 such that p^k divides n.
%e A389367 a(0) = 0 because the Dyck natural number evaluating to 0 is epsilon, which has a semilength of 0.
%e A389367 a(1) = 1 because the Dyck natural number evaluating to 1 is "()", which has a semilength of 1.
%e A389367 a(2) = 2 because the Dyck natural number evaluating to 2 is "(())", which has a semilength of 2.
%e A389367 a(3) = 3 because the Dyck natural number evaluating to 3 is "()(())", which has a semilength of 3.
%e A389367 a(4) = 3 because the Dyck natural number evaluating to 4 is "((()))", which has a semilength of 3.
%o A389367 (Python)
%o A389367 import sys
%o A389367 from sympy import factorint, primerange
%o A389367 def a(n):
%o A389367     return int(len(gamma(n)) // 2)
%o A389367 def gamma(n): # Standard RPF natural spelling function (Def. 2.11 in arXiv:2102.02777)
%o A389367     if n == 0:
%o A389367         return("")
%o A389367     elif n == 1:
%o A389367         return("()")
%o A389367     retVal = ""
%o A389367     for i in reversed(list_of_prime_powers(n)):
%o A389367         if i == 0:
%o A389367             retVal = "()" + retVal
%o A389367         elif i == 1:
%o A389367             retVal = "(())" + retVal
%o A389367         else:
%o A389367             retVal = "(" + gamma(i) + ")" + retVal
%o A389367     return retVal
%o A389367 def list_of_prime_powers(n) -> list[int]:
%o A389367     factors = factorint(n)
%o A389367     gpf = max(factors.keys())
%o A389367     primes = list(primerange(2, gpf + 1))
%o A389367     return [factors.get(p, 0) for p in primes]
%o A389367 print([a(n) for n in range(0,50)])
%Y A389367 Cf. A082582 (with offset 1 appears in Conjecture 5.1 of Childress).
%K A389367 nonn
%O A389367 0,3
%A A389367 _Ralph L. Childress_, Nov 02 2025
