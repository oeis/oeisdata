%I A389353 #31 Oct 15 2025 01:07:37
%S A389353 2,3,5,7,13,71,97,397,739,99137,139339,1139917,779919137,
%T A389353 97191997777319771,3317719713399797917
%N A389353 Primes p which are the concatenation of the last digits of the length(p) primes before and including p, where length(p) is the number of digits of p.
%e A389353 13 is a term since the last digits of 11 and 13 are 1, 3.
%e A389353 139339 is a term since the last digits of the 6 primes before and including it are: 1, 3, 9, 3, 3, 9.
%t A389353 pOK[p_]:=Module[{l=IntegerLength[p],s={IntegerDigits[p][[-1]]}},Do[PrependTo[s,Last[IntegerDigits[NextPrime[p,-i]]]],{i,l-1}];s==IntegerDigits[p]];Select[Prime[Range[10^5]],pOK] (* _James C. McMahon_, Oct 14 2025 *)
%o A389353 (Python)
%o A389353 from sympy import isprime, prevprime
%o A389353 from itertools import count, islice, product
%o A389353 def ok(n):
%o A389353     if not isprime(n): return False
%o A389353     d, p = list(map(int, str(n))), prevprime(n)
%o A389353     for i in range(2, len(d)+1):
%o A389353         if p%10 != d[-i]: return False
%o A389353         p = prevprime(p)
%o A389353     return True
%o A389353 def agen(): # generator of terms
%o A389353     yield from [2, 3, 5, 7]
%o A389353     yield from (k for d in count(2) for t in product("1379", repeat=d) if ok(k:=int("".join(t))))
%o A389353 print(list(islice(agen(), 13))) # _Michael S. Branicky_, Oct 06 2025
%Y A389353 Cf. A055642, A091633.
%K A389353 nonn,base,more
%O A389353 1,1
%A A389353 _Morné Louw_, Oct 01 2025
%E A389353 a(14) from _Michael S. Branicky_, Oct 06 2025
%E A389353 a(15) from _Michael S. Branicky_, Oct 13 2025
