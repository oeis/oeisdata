%I A389243 #48 Nov 23 2025 16:31:49
%S A389243 1,2,3,4,5,6,7,8,9,55,7396,17915,5443996,19202076,21841310627,
%T A389243 61667353048428,5351659167415988
%N A389243 Numbers k whose digits d_1...d_m satisfy k = Sum_{i=1..m} i * d_i^i.
%C A389243 Let k have digits d_1 ... d_m. Define S(k) = Sum_{i=1..m} i * (d_i^i), where d_i is k's i-th decimal digit, counted from the leftmost (most significant) digit of k.
%C A389243 All terms have 62 or fewer digits since Sum_{i=1..m} i * 9^i < 10^(m-1) for all m >= 63. - _Michael S. Branicky_, Oct 28 2025
%C A389243 If k in A009996 and S(k) > k then no number that has the same digits as k counted with multiplicity is in this sequence. For example as S(987) > 987 we have 789, 798 and the other permutations not in the sequence. Also 988, 998 and 999 and their permutations are out. - _David A. Corneth_, Oct 29 2025
%F A389243 {k | S(k) = k} where S(n) = Sum_{i=1..m} i * d_i^i, with n = d_1 d_2 .. d_m.
%e A389243 55 is a term since 1*5^1 + 2*5^2 = 5 + 50 = 55.
%e A389243 5443996 is a term since 1*5^1 + 2*4^2 + 3*4^3 + 4*3^4 + 5*9^5 + 6*9^6 + 7*6^7 = 5443996.
%o A389243 (MATLAB)
%o A389243  function v=weighted_digit_powers
%o A389243  v=[];
%o A389243  for n=1:10000000
%o A389243    ds = num2str(n)-'0'; s = 0;
%o A389243    for i=1:numel(ds)
%o A389243        s = s + i * ds(i)^i;
%o A389243    end
%o A389243    if s==n
%o A389243        v(end+1)=n;
%o A389243    end
%o A389243  end
%o A389243  disp(v)
%o A389243 (Python)
%o A389243 def f(n): return sum(i*d**i for i, d in enumerate(map(int, str(n)), 1))
%o A389243 def ok(n): return f(n) == n
%o A389243 print([k for k in range(10**7) if ok(k)]) # _Michael S. Branicky_, Oct 28 2025
%o A389243 (PARI) isok(k) = my(d=digits(k)); k == sum(i=1, #d, i*d[i]^i); \\ _Michel Marcus_, Nov 23 2025
%Y A389243 Cf. A009996.
%K A389243 nonn,base,fini,more,changed
%O A389243 1,2
%A A389243 _Safwan Jaradat_, Oct 28 2025
%E A389243 a(14)-a(15) from _Michael S. Branicky_, Oct 28 2025
%E A389243 a(16)-a(17) from _Bert Dobbelaere_, Nov 10 2025
