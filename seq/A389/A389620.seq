%I A389620 #36 Jan 14 2026 17:32:52
%S A389620 1,1,3,36,15,12000,735,1367234901661860000,314344800,27829992960,
%T A389620 1205820,8529163613684846032188162555671012748000,241321080
%N A389620 Number of distinct sets F = {P_1, P_2, ..., P_k} where P_i is a faithful permutation representation of Aut(G_i) as defined in the comments, and where G_i is the i-th group of order n.
%C A389620 Let N = {1, ..., n}, let G be a group of order n, and let f: G -> N be a bijection. If phi is an automorphism of G, then f*phi*f^-1 = p_f(phi) is a permutation of N with respect to phi, and the image P = p_f(Aut(G)) is a subgroup of the symmetric group S_n. We define an enumeration of faithful permutation representations of automorphism groups as a set F = {P_1, P_2, ..., P_k}, where P_i is an image of the automorphism group of the i-th group of order n. Not only are there many possible sets F, dependent on choice of image, but F can be defined in at least two different ways, depending on whether the elements of F are distinct or nondistinct. a(n) is the number of sets for the case where each image in F is distinct.
%F A389620 a(n) = Product_{i=1..m} binomial(#Im(Aut(G_i)), r_i), where #Im(Aut(G)) is the number of images of Aut(G) in S_n, r_i is the number of groups G_j such that Aut(G_i) = Aut(G_j), and the product is taken over the m distinct Aut(G_i).
%e A389620 S_4 contains 9 subgroups isomorphic to Aut(C_4) = C_2, and 4 subgroups isomorphic to Aut(C_2 x C_2) = S_3. Therefore a(4) = 9*4 = 36.
%e A389620 The case for n = 8 is the first where two or more isomorphism classes share the same automorphism group: Aut(C_4 x C_2) = Aut(D_8) = D_8. With repetition, the number of images in S_8 for each Aut(G) is 6685, 14070, 14070, 5740, and 360. Using the formula, a(8) = 6685*binomial(14070, 2)*5740*360 = 1367234901661860000.
%o A389620 (GAP)
%o A389620 a := function(n)
%o A389620 local A, prod, S, k, count, G, C, P;
%o A389620 A := []; prod := 1; S := SymmetricGroup(n);
%o A389620 for k in [1..NrSmallGroups(n)] do
%o A389620     count := 0; G := AutomorphismGroup(SmallGroup(n, k));
%o A389620     for C in ConjugacyClassesSubgroups(S) do
%o A389620         P := Representative(C);
%o A389620         if IsomorphismGroups(P, G) <> fail then
%o A389620             count := count + Size(C);
%o A389620         fi;
%o A389620     od;
%o A389620     Add(A, count);
%o A389620 od;
%o A389620 for n in Unique(A) do
%o A389620     prod := prod*Binomial(n, Length(Positions(A, n)));
%o A389620 od;
%o A389620 return prod;
%o A389620 end;
%Y A389620 Cf. A005432, A385480.
%K A389620 nonn,more
%O A389620 1,3
%A A389620 _Miles Englezou_, Jan 12 2026
