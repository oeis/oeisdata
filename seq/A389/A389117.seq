%I A389117 #22 Sep 25 2025 10:45:28
%S A389117 3,7,0,9,7,5,1,2,3,3,8,9,9,2,2,2,3,6,8,8,8,7,9,6,1,4,6,6,8,4,2,0,7,8,
%T A389117 8,8,2,1,7,4,4,3,6,4,5,0,6,4,1,6,9,7,6,6,8,0,9,7,8,9,0,9,9,3,9,3,9,4,
%U A389117 4,8,1,4,6,5,3,8,6,4,4,8,2,2,2,6,8,1,2,3,7,0,1,8,0,6,2,7,1,7,0,5,2,3,5,7,4
%N A389117 Decimal expansion of the sum of the distinct entries of 1/A055204(n)^(1/2).
%C A389117 A result of Erdős and Selfridge implies that A055204(n) only repeats at squares, so one can simply sum  1/A055204(n)^(1/2) for all n other than squares greater than 1. The number of elements of A387186 up to x is asymptotic to this constant times x^(1/2). Conjecturally, the same is true for A388851 (see also Erdős problem #374). The series converges at an exponentially fast rate.
%H A389117 Thomas Bloom, <a href="https://www.erdosproblems.com/374">Problem 374</a>, Erdős Problems.
%H A389117 P. Erdős and R. L. Graham, <a href="https://old.renyi.hu/~p_erdos/1976-25.pdf">On products of factorials</a>, Bull. Inst. Math. Acad. Sinica 4 (1976), pp. 337-355.
%e A389117 3.70975123389922236888796146684207888217443645...
%p A389117 split := r -> ListTools:-Reverse(convert(floor(r), base, 10)):
%p A389117 core := n -> n / expand(numtheory:-nthpow(n, 2)):
%p A389117 A389117 := proc(upto) local total, cofact, n, term;
%p A389117   total := 0.0; cofact := 1;
%p A389117   for n from 2 to upto do
%p A389117       cofact := core(cofact * n);
%p A389117       if not issqr(n) then
%p A389117           term := evalf(1 / sqrt(cofact));
%p A389117           total := total + term;
%p A389117       fi;
%p A389117   od;
%p A389117 1 + total end:
%p A389117 Digits := 200: A389117(1000): split(% * 10^104);  # _Peter Luschny_, Sep 24 2025
%o A389117 (PARI) 1 + suminf(n=2, if(issquare(n), 0, 1/sqrt(core(n!)))) \\ _Hugo Pfoertner_, Sep 23 2025
%Y A389117 Cf. A055204, A387186, A388851.
%K A389117 nonn,cons
%O A389117 1,1
%A A389117 _Terence Tao_, Sep 23 2025
