%I A389839 #24 Nov 03 2025 10:46:28
%S A389839 6,8,12,16,20,28,32,42,48,60,68
%N A389839 Smallest even number which cannot be written as the difference of two consecutive numbers which are relatively prime to the primorial prime(n)#.
%C A389839 By symmetry (since x is relatively prime to prime(n)# if and only if prime(n)# - x is), periodicity modulo prime(n)#, and the fact that prime(n)#/2 - 2 is relatively prime to prime(n)#, it is sufficient to check only the numbers relatively prime to prime(n)# that are bounded by prime(n)#/2 + 2, when starting from -1 (to also account for the difference between k*prime(n)# - 1 and k*prime(n)# + 1).
%H A389839 Thomas Bloom, <a href="https://www.erdosproblems.com/854">Problem 854</a>, Erdős Problems.
%e A389839 For n=3, a(3)=8 since since the numbers relatively prime to and bounded by prime(3)# = 30 are in the set {1, 7, 11, 13, 17, 19, 23, 29}. The consecutive differences are 6,4,2,4,2,4,6,2 (which then repeat). Hence 2 (also appears as 31-29), 4 and 6 do appear, but 8 does not, so a(3)=8.
%e A389839 For n=6, the differences that do appear are {2, 4, 6, 8, 10, 12, 14, 16, 18, 22}. Hence 20 is the smallest even difference not appearing.
%o A389839 (SageMath)
%o A389839 def a(n):
%o A389839     N=prod(Primes()[0:n])
%o A389839     R=[a for a in range(-1,ceil(N/2)+3,2) if gcd(N,a)==1]
%o A389839     A=set([R[i]-R[i-1] for i in range(1,len(R))])
%o A389839     i=2
%o A389839     while i in A:
%o A389839         i+=2;
%o A389839     return i
%o A389839 (Python)
%o A389839 from math import gcd
%o A389839 from sympy import primorial
%o A389839 def A389839(n):
%o A389839     c, s, q, p = 0, {0}, -1, primorial(n)
%o A389839     for i in range(1,(p>>1)+3,2):
%o A389839         if gcd(p,i) == 1:
%o A389839             r = i-q
%o A389839             if r not in s:
%o A389839                 s.add(r)
%o A389839                 while c in s:
%o A389839                     c += 2
%o A389839             q = i
%o A389839     return c # _Chai Wah Wu_, Oct 29 2025
%Y A389839 Cf. A051271, A051272, A002110, A007310, A007775, A008364-A008366, A166061, A166063.
%K A389839 nonn,more
%O A389839 2,1
%A A389839 _Stijn Cambie_, Oct 16 2025
%E A389839 a(10) from _Andrew Howroyd_, Oct 16 2025
%E A389839 a(11) from _Chai Wah Wu_, Oct 29 2025
%E A389839 a(12) from _Chai Wah Wu_, Nov 03 2025
