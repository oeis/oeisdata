%I A389588 #14 Oct 16 2025 17:54:19
%S A389588 4,4,8,8,16,16,16,38,50,62,66,66,86,94,140,144,160,172,192,212,216,
%T A389588 240,298,310,346,366,390,402,422,512,528,564,588,620,660,664,704,794,
%U A389588 850,882,898,934,966,1014,1144,1160,1204,1236,1284,1332,1384,1424,1554,1598,1634,1698,1734,1786,1822,1992,2056,2100,2152,2204
%N A389588 Number of integer lattice points (x,y) with x^2 + y^2 <= n^2 that lie outside the interior of the regular hexagon inscribed in the circle of radius n centered at the origin.
%C A389588 The hexagon has flat-top orientation with vertices at (+-n,0), (+-n/2, +-sqrt(3)*n/2). Boundary points of the hexagon are included in the count. This sequence explores the geometric mismatch between the circular symmetry of the circle and the hexagonal symmetry on the integer lattice.
%F A389588 a(n) = A000328(n) - (number of lattice points strictly inside the inscribed hexagon).
%F A389588 a(n) = #{ (x,y) in Z^2 : x^2+y^2 <= n^2 and not( |y| < sqrt(3)*n/2 and |sqrt(3)*x+y| < sqrt(3)*n and |sqrt(3)*x-y| < sqrt(3)*n ) }.
%F A389588 a(n) = A000328(n) - I(n) where I(n) = 2*n - 2*m - 1 + 4 * Sum_{k=1..m} ceiling(n-k/sqrt(3)) with m=floor(sqrt(3)*n/2). - _Sean A. Irvine_, Oct 13 2025
%e A389588 For n=1:
%e A389588   Circle points: (0,0),(+-1,0),(0,+-1) -> 5 points.
%e A389588   Hexagon interior test uses inequalities (with sqrt(3) \approx 1.732):
%e A389588   |y| < 0.866, |sqrt(3)*x+y| < 1.732, |sqrt(3)*x-y| < 1.732.
%e A389588   Check:
%e A389588   (0,0): all satisfied -> inside hexagon -> not counted.
%e A389588   (1,0): |sqrt(3)*1+0|=1.732 not <1.732 -> counted.
%e A389588   (-1,0): |sqrt(3)*(-1)+0|=1.732 not <1.732 -> counted.
%e A389588   (0,1): |y|=1 not <0.866 -> outside -> counted.
%e A389588   (0,-1): |y|=1 not <0.866 -> outside -> counted.
%e A389588   Therefore 4 points remain: (+-1,0),(0,+-1). So a(1)=4.
%e A389588 For n=2:
%e A389588   Circle contains 13 lattice points. The hexagon interior contains 9 of them; only (+-2,0),(0,+-2) lie outside. So a(2)=4.
%o A389588 (MATLAB)
%o A389588 % a(n) = Number of integer lattice points (x,y) with x^2 + y^2 <= n^2
%o A389588 %        that lie outside the interior of the inscribed regular hexagon
%o A389588 %        (flat-top orientation). Hexagon boundary points are counted.
%o A389588 function val = a(n)
%o A389588     count = 0;
%o A389588     s3 = sqrt(3);
%o A389588     for x = -n:n
%o A389588         for y = -n:n
%o A389588             if x^2 + y^2 <= n^2
%o A389588                 % Check if point lies in the strict interior of the hexagon
%o A389588                 in_hex = (abs(y) < (s3/2)*n) && ...
%o A389588                          (abs(s3*x + y) < s3*n) && ...
%o A389588                          (abs(s3*x - y) < s3*n);
%o A389588                 if ~in_hex
%o A389588                     count = count + 1;
%o A389588                 end
%o A389588             end
%o A389588         end
%o A389588     end
%o A389588     val = count;
%o A389588 end
%o A389588 % Example: generate first 30 terms
%o A389588 for n = 1:30
%o A389588     fprintf('%d ', a(n));
%o A389588 end
%o A389588 fprintf('\n');
%Y A389588 Cf. A000328 (circle lattice points).
%K A389588 nonn
%O A389588 1,1
%A A389588 _Safwan Jaradat_, Oct 08 2025
