%I A389360 #55 Oct 24 2025 12:44:38
%S A389360 4,6,9,12,75,30,70,56,2403,280,3465,210,793,4732,3213,1456,31110,612,
%T A389360 67203,145540,464646,2640,476938,21000,86550,234026,1053702,34776,
%U A389360 37584001,8100,2301456,77780756,61924632,26515138,105846930,665280,622999377,233999782,510752034,1154440
%N A389360 Smallest m >= 2*n such that binomial(m,n) is a multiple of m-i for all 0<=i<n, but one.
%C A389360 Other known values: a(43) = 24458112 and a(47) = 3076480.
%H A389360 Chai Wah Wu, <a href="/A389360/b389360.txt">Table of n, a(n) for n = 2..59</a>
%H A389360 Thomas Bloom, <a href="https://www.erdosproblems.com/1063">Problem 1063</a>, Erdős Problems.
%o A389360 (SageMath)
%o A389360 def a(n):
%o A389360     m=2*n;
%o A389360     while 0<1:
%o A389360         t=binomial(m, n); S=[i for i in range(0, n) if t%(m-i)!=0];
%o A389360         if len(S)==1: return m;
%o A389360         else: m+=1
%o A389360 (PARI) isok(m,n) = my(b=binomial(m,n), nb=0); for (i=0, n-1, if (!(b % (m-i)), nb++)); nb==n-1;
%o A389360 a(n) = my(m=2*n); while (!isok(m,n), m++); m; \\ _Michel Marcus_, Oct 09 2025
%o A389360 (Python)
%o A389360 from itertools import count
%o A389360 from math import comb
%o A389360 def A389360(n):
%o A389360     c = comb(n<<1,n)
%o A389360     for m in count(n<<1):
%o A389360         s = 0
%o A389360         for i in range(n):
%o A389360             if c%(m-i):
%o A389360                 s += 1
%o A389360                 if s>1:
%o A389360                     break
%o A389360         if s==1:
%o A389360             return m
%o A389360         c = c*(m+1)//(m+1-n) # _Chai Wah Wu_, Oct 14 2025
%Y A389360 Cf. A007318.
%K A389360 nonn
%O A389360 2,1
%A A389360 _Stijn Cambie_, Oct 01 2025
%E A389360 a(38)-a(40) from _Chai Wah Wu_, Oct 14 2025
%E A389360 a(41) from _Stijn Cambie_, Oct 01 2025
