%I A085499 #23 Nov 25 2025 12:39:56
%S A085499 3,5,7,17,19,31,53,103,127,271,367,463,499,859,967,1013,1483,1951,
%T A085499 3229,3533,3769,3833,4373,5477,6101,7069,7457,8191,8501,9041,9521,
%U A085499 11527,11621,11777,13121,14551,17791,20071,21943,23167,25471,29311,33619,36979,44491,45667
%N A085499 Primes p having exactly one partition into distinct divisors of p+1.
%H A085499 Robert Israel, <a href="/A085499/b085499.txt">Table of n, a(n) for n = 1..300</a> (first 68 terms from Amiram Eldar)
%F A085499 A085496(a(n)) = 1.
%p A085499 rep:= proc(n,S) # true iff n is representable as a sum of elements of set S
%p A085499   option remember;
%p A085499   local s, Sp;
%p A085499   if member(n,S) then return true fi;
%p A085499   Sp:= select(t -> t < n, S);
%p A085499   s:= convert(Sp,`+`);
%p A085499   if s = n then return true
%p A085499   elif s < n then return false
%p A085499   fi;
%p A085499   s:= max(Sp);
%p A085499   Sp:= Sp minus {s};
%p A085499   rep(n-s, Sp) or rep(n, Sp)
%p A085499 end proc:
%p A085499 frep:= proc(n,S) # 0 if no reps, 1 if unique rep, 2 if more than one rep
%p A085499 option remember;
%p A085499   local s, Sp, r;
%p A085499   Sp:= select(t -> t <= n, S);
%p A085499   s:= convert(Sp,`+`);
%p A085499   if s = n then return 1
%p A085499   elif s < n then return 0
%p A085499   fi;
%p A085499   s:= max(Sp);
%p A085499   Sp:= Sp minus {s};
%p A085499   r:= frep(n-s,Sp);
%p A085499   if r = 0 then frep(n,Sp)
%p A085499   elif r = 1 then if rep(n,Sp) then 2 else 1 fi
%p A085499   else 2
%p A085499   fi
%p A085499 end proc:
%p A085499 select(p -> frep(p, numtheory:-divisors(p+1))=1, [seq(ithprime(i),i=1..10000)]); # _Robert Israel_, Nov 25 2025
%t A085499 seqQ[p_] := Module[{d = Most[Divisors[p+1]]}, SeriesCoefficient[Series[Product[1 + x^d[[i]], {i, Length[d]}], {x, 0, p}], p]  == 1]; Select[Range[1000], PrimeQ[#] && seqQ[#] &] (* _Amiram Eldar_, Jan 13 2020 *)
%Y A085499 Subsequence of A085498 and of A085494.
%Y A085499 Cf. A085496.
%K A085499 nonn,changed
%O A085499 1,1
%A A085499 _Reinhard Zumkeller_, Jul 03 2003
%E A085499 a(14)-a(38) from _Alois P. Heinz_, Apr 30 2012
%E A085499 a(39)-a(46) from _Amiram Eldar_, Jan 13 2020
