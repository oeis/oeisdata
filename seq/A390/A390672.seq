%I A390672 #24 Nov 21 2025 20:03:36
%S A390672 10,1100,101100,111000,10101100,11001100,1010101100,11011000,10111000,
%T A390672 1100101100,101010101100,1110001100,10101010101100,110010101100,
%U A390672 1011001100,11110000,1010101010101100,1100111000,101010101010101100,111000101100,101100101100,11001010101100
%N A390672 Nonzero Dyck natural numbers encoded as binary integers.
%C A390672 The Dyck natural numbers form a subset of the Dyck language in which each word uniquely represents a nonnegative integer.  The representation arises from a recursive generalization of prime factorization defined by the standard RPF natural spelling function gamma_{N_r} (here shortened to simply "gamma" for readability), according to Definition 2.11 in arXiv:2102.02777: gamma(0) = epsilon, where epsilon denotes the empty string (length = 0) and gamma(1) = "()".
%C A390672 For n > 1, write n as a product of powers of consecutive primes from 2 up to and including the greatest prime factor gpf(n).
%C A390672 Each exponential term prime(k)^m is enclosed in parentheses, and if m > 0 it is replaced recursively by gamma(m).
%C A390672 After all replacements, discard all non-parenthesis symbols; the remaining parentheses form gamma(n).
%C A390672 In this sequence, the Dyck natural numbers are encoded in base-2 by substituting 1 for "(" and 0 for ")".
%C A390672 Since gamma(0) = epsilon (the empty string), there are no symbols to be mapped to binary digits to yield a(0); thus the first term of this sequence corresponds to gamma(1) rather than gamma(0), and the sequence accordingly has an offset of 1.
%C A390672 Replacing 1 -> U and 0 -> D, this sequence corresponds to the set of nontrivial Dyck paths d that avoid DUDD and do not end with DUD.  See Theorem 2.7 in arXiv:2102.02777.
%H A390672 Ralph L. Childress, <a href="/A390672/b390672.txt">Table of n, a(n) for n = 1..3570</a>
%H A390672 Ralph L. Childress, <a href="https://arxiv.org/abs/2102.02777">Recursive Prime Factorizations: Dyck Words as Representations of Numbers</a>, arXiv:2102.02777 [cs.FL], 2021. See especially sections 2.1 and 2.3, as well as Table 1.
%e A390672 a(1) = 10 because the Dyck natural number evaluating to 1 is ().
%e A390672 a(2) = 1100 because the Dyck natural number evaluating to 2 is (()).
%e A390672 a(3) = 101100 because the Dyck natural number evaluating to 3 is ()(()).
%e A390672 a(4) = 111000 because the Dyck natural number evaluating to 4 is ((())).
%o A390672 (Python)
%o A390672 from sympy import factorint, primerange
%o A390672 def a(n):
%o A390672     return gamma(n).replace("(","1").replace(")","0")
%o A390672 def gamma(n): # Standard RPF natural spelling function (Def. 2.11 in arXiv:2102.02777)
%o A390672     if n == 0:
%o A390672         return("")
%o A390672     elif n == 1:
%o A390672         return("()")
%o A390672     retVal = ""
%o A390672     for i in reversed(list_of_prime_powers(n)):
%o A390672         if i == 0:
%o A390672             retVal = "()" + retVal
%o A390672         elif i == 1:
%o A390672             retVal = "(())" + retVal
%o A390672         else:
%o A390672             retVal = "(" + gamma(i) + ")" + retVal
%o A390672     return retVal
%o A390672 def list_of_prime_powers(n) -> list[int]:
%o A390672     factors = factorint(n)
%o A390672     gpf = max(factors.keys())
%o A390672     primes = list(primerange(2, gpf + 1))
%o A390672     return [factors.get(p, 0) for p in primes]
%o A390672 print([a(n) for n in range(1, 23)])
%Y A390672 Cf. A389367 (semilengths of the Dyck natural numbers).
%K A390672 nonn,base,new
%O A390672 1,1
%A A390672 _Ralph L. Childress_, Nov 14 2025
