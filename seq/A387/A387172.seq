%I A387172 #14 Oct 14 2025 16:45:08
%S A387172 160,567,1600,6250,7203,8704,11907,16000,24057,26411,57344,62500,
%T A387172 90112,102487,151263,160000,250047,265625,295936,303104,485537,625000,
%U A387172 793881,802816,1015625,1085773,1600000,1982464,2033647,2235331,2539107,2997383,3176523,5250987,5316979
%N A387172 Numbers k = p1^e1*p2^e2, with e1 != e2, such that the Euclidean distance between points (p1, e1) and (p2, e2) is an integer.
%H A387172 Felix Huber, <a href="/A387172/b387172.txt">Table of n, a(n) for n = 1..10000</a>
%F A387172 If t is a term, then t*(p_1*p_2)^m = t*A007947(t)^m is also a term for any positive integer m.
%e A387172 160 = 2^5*5^1 has exactly two prime-power factors with distinct exponents and the lattice distance between (2, 5) and (5, 1) is an integer: sqrt((5 - 2)^2 + (1 - 5)^2) = 5. Hence, 160 is a term.
%p A387172 A387172:=proc(N) # To get all terms <= N
%p A387172     local p,i,j,k,l,m,x,y,u,v;
%p A387172     l:=[];
%p A387172     for x from 2 while min(2^(2*x+1)*(x^2+1),2^(x^2)*(2*x+2))<=N do
%p A387172         for y to x-1 do
%p A387172             if gcd(x,y)=1 and is(x-y,odd) then
%p A387172                 u:=x^2-y^2;
%p A387172                 v:=2*x*y;
%p A387172                 for i while min(2^(u*i+1)*(v*i+2),2^(v*i+1)*(u*i+2))<=N do
%p A387172                     l:=[op(l),i*[v,u]];
%p A387172                     l:=[op(l),i*[u,v]];
%p A387172                 od
%p A387172             fi
%p A387172         od
%p A387172     od;
%p A387172     m:=[];
%p A387172     for k in l do
%p A387172         for i while ithprime(i)^(k[1]+1)*(ithprime(i)+k[2])<=N do
%p A387172             p:=ithprime(i);
%p A387172             for j while p^(k[1]+j)*(p+k[2])^j<=N do
%p A387172                 if isprime(p+k[2]) then
%p A387172                     m:=[op(m),p^(k[1]+j)*(p+k[2])^j];
%p A387172                     if p^j*(p+k[2])^(k[1]+j)<=N then
%p A387172                         m:=[op(m),p^j*(p+k[2])^(k[1]+j)];
%p A387172                     fi
%p A387172                 fi
%p A387172             od
%p A387172         od
%p A387172     od;
%p A387172     return op(sort(m))
%p A387172 end proc;
%p A387172 A387172(5316979);
%Y A387172 Subsequence of A007774.
%Y A387172 Cf. A007947, A389339, A389340.
%K A387172 nonn
%O A387172 1,1
%A A387172 _Felix Huber_, Oct 06 2025
