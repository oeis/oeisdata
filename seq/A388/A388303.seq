%I A388303 #26 Sep 18 2025 00:45:49
%S A388303 0,0,1,1,2,1,4,1,2,2,4,1,3,1,4,4,2,1,3,1,3,4,4,1,3,2,4,2,3,1,4,1,2,4,
%T A388303 4,4,5,1,4,4,3,1,4,1,3,3,4,1,3,2,3,4,3,1,3,4,3,4,4,1,3,1,4,3,2,4,4,1,
%U A388303 3,4,4,1,3,1,4,3,3,4,4,1,3,2,4,1,3,4,4
%N A388303 A classification of the nonnegative integers, where each number lies in exactly one of six classes.
%C A388303 Although this is a list, we set the offset to 0 for more intuitive usage: a(n) is the index of the class to which the integer n belongs. There are six mutually exclusive classes, and the nonnegative integers are the union of these classes.
%C A388303 The six classes are defined as follows: n is in class C0 <=> n in {0, 1}. For n >= 2, let F(n) represent the canonical prime factorization of n, p the sequence of prime factors in F(n), and e the corresponding sequence of exponents. Define three Boolean expressions: L = [card(p) = 1], M = [max(e) = 1], and E = [card(set(e)) = 1], where [.] denotes the Iverson bracket. Then n is in C1 <=> L and E and M; n is in C2 <=> L and E and not M; n is in C3 <=> not L and not E and not M; n is in C4 <=> not L and E and M; n is in C5 <=> not L and E and not M. This amounts to the mapping: C0 -> A387487, C1 -> A000040, C2 -> A246547, C3 -> A059404, C4 -> A120944, C5 -> A303606. Finally a(n) = k <=> n in Ck; k = 0..5.
%H A388303 Michael De Vlieger, <a href="/A388303/b388303.txt">Table of n, a(n) for n = 0..10000</a>
%H A388303 Peter Luschny, <a href="/A388303/a388303.txt">Python implementation</a>.
%e A388303 |n|  |L|    |M|   |E|  |class|
%e A388303 ----------------------------------------
%e A388303 32:  True  False True   [C2] -> A246547;
%e A388303 36:  False False True   [C5] -> A303606;
%e A388303 42:  False True  True   [C4] -> A120944;
%e A388303 43:  True  True  True   [C1] -> A000040;
%e A388303 44:  False False False  [C3] -> A059404.
%t A388303 Table[Which[n < 2, 0, PrimeQ[n], 1, PrimePowerQ[n], 2, SquareFreeQ[n], 4, Length[Union@ FactorInteger[n][[;; , -1]]] == 1, 5, True, 3], {n, 0, 86}] (* _Michael De Vlieger_, Sep 17 2025 *)
%o A388303 (Python)  # Cf. links.
%o A388303 (SageMath)
%o A388303 def a(n: int) -> int:
%o A388303     if n < 2: return 0
%o A388303     factors = list(factor(n))
%o A388303     exponents = [f[1] for f in factors]
%o A388303     L = 1 == len(factors)
%o A388303     E = 1 == len(set(exponents))
%o A388303     M = 1 == max(exponents)
%o A388303     if L and E: return 1 if M else 2
%o A388303     if E: return 4 if M else 5
%o A388303     return 3
%Y A388303 Cf. A001477, A387487, A000040, A246547, A059404, A120944, A303606.
%K A388303 nonn,easy
%O A388303 0,5
%A A388303 _Peter Luschny_, Sep 16 2025
