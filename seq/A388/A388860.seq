%I A388860 #29 Sep 26 2025 04:06:55
%S A388860 0,1,2,4,5,8,10,11,16,20,21,22,32,40,42,43,44,45,64,80,84,86,88,89,90,
%T A388860 128,160,168,169,171,172,176,178,180,256,320,336,338,339,342,344,345,
%U A388860 352,356,357,360,361,373,512,597,640,672,676,677,678,679,681
%N A388860 Numbers k such that the number of runs of 1's in k is the same as the number of runs on 1's in k^2.
%C A388860 Numbers k such that A069010(k) = A069010(k^2).
%C A388860 If k is a term, so is 2^t*k for t >= 0. In particular, 2^t is a term for all t >= 0.
%H A388860 Ahmet Caglar Saygili, <a href="/A388860/b388860.txt">Table of n, a(n) for n = 1..10000</a>
%e A388860 45 is a term since 45 = 101101_2 has A069010(45) = 3 and 45^2 = 2025 = 11111101001_2 has A069010(2025) = 3.
%e A388860 33 is not a term since 33 = 100001_2 so A069010(33) = 2 but 33^2 = 1089 = 10001000001_2 so A069010(1089) = 3.
%t A388860 q[n_] := Equal @@ (Ceiling[Length[Split[IntegerDigits[#, 2]]]/2] & /@ {n, n^2}); Select[Range[0, 700], q] (* _Amiram Eldar_, Sep 22 2025 *)
%o A388860 (Julia)
%o A388860 runs1(n)=begin
%o A388860   b=string(n,base=2); r=0; p='0'
%o A388860   for c in b
%o A388860     if c=='1' && p=='0'; r+=1; end
%o A388860     p=c
%o A388860   end
%o A388860   r
%o A388860 end
%o A388860 [ m for m in 0:681 if runs1(m^2)==runs1(m) ]
%o A388860 (Python)
%o A388860 from itertools import groupby
%o A388860 def b(n): return sum(1 for k, g in groupby(bin(n)[2:]) if k == '1')
%o A388860 def ok(n): return b(n) == b(n**2)
%o A388860 print([k for k in range(700) if ok(k)]) # _Michael S. Branicky_, Sep 25 2025
%Y A388860 Cf. A069010, A077436.
%K A388860 nonn,base
%O A388860 1,3
%A A388860 _Ahmet Caglar Saygili_, Sep 21 2025
