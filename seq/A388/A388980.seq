%I A388980 #26 Sep 26 2025 12:08:09
%S A388980 2,8,12,18,20,28,32,44,48,50,52,68,72,76,80,92,98,108,112,116,124,128,
%T A388980 148,162,164,172,176,180,188,192,200,208,212,236,242,244,252,268,272,
%U A388980 284,288,292,300,304,316,320,332,338,356,368,388,392,396,404,412,428,432,436,448,450,452,464,468,496,500,508,512
%N A388980 Even numbers of the form p * m^2, where p is prime and m > 0.
%C A388980 Even numbers k such that A007913(k) is a prime.
%H A388980 Antti Karttunen, <a href="/A388980/b388980.txt">Table of n, a(n) for n = 1..10000</a>
%p A388980 M:= 1000: # for terms <= M
%p A388980 P:= select(isprime,{seq(i,i=3..M/4,2)}):
%p A388980 R:= {seq(2*m^2,m=1..floor(sqrt(M/2)))} union map(proc(p) local m; seq(p*m^2,m=2..floor(sqrt(M/p)),2) end proc, P):
%p A388980 sort(convert(R,list)); # _Robert Israel_, Sep 25 2025
%o A388980 (PARI) is_A388980(k) = (!(k%2) && isprime(core(k)));
%o A388980 (PARI)
%o A388980 up_to = 16384;
%o A388980 A388980list(up_to) = { my(v=vector(up_to), i=0); forstep(n=2,oo,2,if(isprime(core(n)), i++; v[i] = n; if(i==up_to, return(v)))); };
%o A388980 v388980 = A388980list(up_to);
%o A388980 A388980(n) = v388980[n];
%o A388980 (Python)
%o A388980 from math import isqrt
%o A388980 from sympy import primepi
%o A388980 from oeis_sequences.OEISsequences import bisection
%o A388980 def A388980(n):
%o A388980     def f(x): return n+x-(isqrt(x>>1)+1>>1)-sum(primepi(x//y**2) for y in range(2,isqrt(x)+1,2))
%o A388980     return bisection(f,n,n) # _Chai Wah Wu_, Sep 22 2025
%o A388980 (Python)
%o A388980 from sympy import isprime
%o A388980 from sympy.ntheory.factor_ import core
%o A388980 def is_A388980(k): return (k%2 == 0 and isprime(core(k))) # _Robert C. Lyons_, Sep 25 2025
%Y A388980 Even terms of A229125. After the initial 2 also the even terms of A228056.
%Y A388980 The topmost row of A388982.
%Y A388980 Cf. A007913.
%K A388980 nonn
%O A388980 1,1
%A A388980 _Antti Karttunen_, Sep 22 2025
