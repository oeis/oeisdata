%I A365097 #34 Sep 29 2023 02:15:05
%S A365097 2,4,25,181,421,3930,8177,102772,199981,3179142,5971945,143610511,
%T A365097 210826981,4754446846,8589934561
%N A365097 Smallest k > 1 such that the total number of 1 digits required to write the numbers 1..k in base n is equal to k.
%C A365097 a(10) = A014778(3), being the smallest term > 1 there.
%C A365097 An upper bound is a(n) <= A226238(n) = u, since the digits of u show there are u 1's in numbers 1..u (in base n). - _Kevin Ryde_, Sep 28 2023
%e A365097 For n=2, the first two natural numbers are {1,10}, which have two 1s.
%e A365097 For n=3, the first k=4 numbers {1,2,10,11} in base 3, have 4 digit 1's, which is equal to k.
%e A365097 For n=4, 25 1s occur in the first 25 natural numbers {1,2,3,10,11,12,13,20,etc.}.
%t A365097 a[n_] := Module[{k = 1, sum = 1}, While[sum == 1 || sum != k, k++; sum += Count[IntegerDigits[k, n], 1]]; k]; Array[a, 6, 2] (* _Amiram Eldar_, Aug 29 2023 *)
%o A365097 (Python)
%o A365097 from itertools import count
%o A365097 from sympy.ntheory.factor_ import digits
%o A365097 def A365097(n):
%o A365097     c, a, q, m = 1, 1, 0, 1
%o A365097     for k in count(2):
%o A365097         m += 1
%o A365097         if m == n:
%o A365097             m = 0
%o A365097             q += 1
%o A365097             a = digits(q,n).count(1)
%o A365097         elif m==1:
%o A365097             a += 1
%o A365097         elif m==2:
%o A365097             a -= 1
%o A365097         c += a
%o A365097         if c == k:
%o A365097             return k # _Chai Wah Wu_, Sep 28 2023
%Y A365097 Cf. A014778, A226238.
%K A365097 nonn,base,more,new
%O A365097 2,1
%A A365097 _Andrew Pope_, Aug 21 2023
%E A365097 a(11)-a(15) from _Amiram Eldar_, Aug 29 2023
%E A365097 a(16) from _Chai Wah Wu_, Sep 28 2023
