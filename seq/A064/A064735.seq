%I A064735 #29 Feb 16 2026 12:09:02
%S A064735 23,13,53,17,113,113,173,191,223,229,131,137,241,431,347,353,359,461,
%T A064735 167,271,173,179,283,389,197,1013,1031,5107,1091,2113,1277,1319,1373,
%U A064735 1399,1493,1151,1571,1163,3167,1733,2179,1181,1913,1193,1973,1993,2111
%N A064735 Next prime containing prime(n) in decimal notation.
%H A064735 Michael S. Branicky, <a href="/A064735/b064735.txt">Table of n, a(n) for n = 1..10000</a> (terms 1..1000 from Harry J. Smith)
%F A064735 a(n) = A252629(n) + prime(n). - _Zak Seidov_, Dec 19 2014
%e A064735 a(1) = 23 since prime(1) = 2 and 23 is the smallest prime > 2 containing 2 as a substring of its decimal digits.
%o A064735 (PARI) a(n)={ my(q=prime(n), m=10^(logint(q,10)+1)); forprime(p=m, oo, my(x=p); while(x>=q, if(x%m==q, return(p)); x\=10)) } \\ _Harry J. Smith_, Sep 24 2009
%o A064735 (PARI) a(n) = my(p=prime(n), q=nextprime(p+1)); while (#strsplit(Str(q),Str(p)) <2, q=nextprime(q+1)); q; \\ _Michel Marcus_, Feb 16 2026
%o A064735 (Python)
%o A064735 from sympy import nextprime, prime
%o A064735 def a(n):
%o A064735     pn = prime(n)
%o A064735     target, p = str(pn), pn
%o A064735     while target not in str(p:=nextprime(p)): pass
%o A064735     return p
%o A064735 print([a(n) for n in range(1, 50)]) # _Michael S. Branicky_, Feb 16 2026
%o A064735 (Python) # faster for initial segment of sequence / b-file
%o A064735 from gmpy2 import digits, next_prime
%o A064735 from itertools import islice
%o A064735 def agen(): # _Michael S. Branicky_, Feb 16 2026
%o A064735     pn = 2
%o A064735     while True:
%o A064735         target, p = digits(pn), pn
%o A064735         while target not in digits(p:=next_prime(p)): pass
%o A064735         yield int(p)
%o A064735         pn = next_prime(pn)
%o A064735 print(list(islice(agen(), 50))) # _Michael S. Branicky_, Feb 16 2026
%Y A064735 Cf. A062584, A030670, A000040, A252629.
%K A064735 base,nonn,changed
%O A064735 1,1
%A A064735 _Reinhard Zumkeller_, Oct 17 2001
