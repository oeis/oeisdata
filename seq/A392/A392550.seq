%I A392550 #22 Jan 29 2026 17:37:50
%S A392550 1,2,44,35,4355,36995,43475,395219,1234211,1585451,18713435
%N A392550 Smallest k for which the number of divisors d of k such that tau(d*k) = tau(k/d + d) is equal to n, where tau = A000005.
%e A392550 a(3) = 35 since its divisors are 1, 5, 7 and 35 and the condition is true for 3 of these:
%e A392550   tau(1*35) = tau(35) != tau(36) = tau(35/1+1), so condition is false;
%e A392550   tau(5*35) = tau(175) = tau(12) = tau(35/5+5), so condition is true;
%e A392550   tau(7*35) = tau(245) = tau(12) = tau(35/7+7), so condition is true;
%e A392550   tau(35*35) = tau(1225) = tau(36) = tau(35/35+35), so condition is true.
%e A392550 No smaller number has a count of 3 such divisors.
%t A392550 seq[len_] := Module[{v = Table[0, {len}], k = 1, c = 0, i}, While[c < len, i = DivisorSum[k, 1 &, DivisorSigma[0, #*k] == DivisorSigma[0, k/# + #] &] + 1; If[i <= len && v[[i]] == 0, c++; v[[i]] = k]; k++]; v]; seq[7] (* _Amiram Eldar_, Jan 16 2026 *)
%o A392550 (PARI) isok(k,n) = sumdiv(k, d, numdiv(d*k) == numdiv(k/d + d)) == n;
%o A392550 a(n) = my(k=1); while (!isok(k,n), k++); k; \\ _Michel Marcus_, Jan 16 2026
%o A392550 (Python)
%o A392550 from itertools import count, islice
%o A392550 from sympy import divisors, divisor_count as tau
%o A392550 def f(k): return sum(1 for d in divisors(k, generator=True) if tau(d*k) == tau(k//d + d))
%o A392550 def agen(): # generator of terms
%o A392550     adict, n = dict(), 0
%o A392550     for k in count(1):
%o A392550         v = f(k)
%o A392550         if v not in adict:
%o A392550             adict[v] = k
%o A392550             while n in adict: yield adict[n]; n += 1
%o A392550 print(list(islice(agen(), 7))) # _Michael S. Branicky_, Jan 16 2026
%Y A392550 Cf. A000005, A391496, A392347.
%K A392550 nonn,more
%O A392550 0,2
%A A392550 _Juri-Stepan Gerasimov_, Jan 15 2026
%E A392550 a(9)-a(10) from _Michel Marcus_, Jan 16 2026
