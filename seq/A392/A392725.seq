%I A392725 #24 Jan 26 2026 11:33:35
%S A392725 1,0,2,5,1,-4,2,9,1,-8,2,13,25,12,-2,13,29,12,-6,13,33,54,32,9,-15,10,
%T A392725 -16,-43,-71,-42,-12,-43,-75,-42,-8,27,-9,-46,-8,31,71,112,154,197,
%U A392725 153,108,62,109,157,108,58,7,-45,-98,-152,-97,-41
%N A392725 a(1) = 1; if the digit sum of |a(n)| is odd, then a(n+1) = a(n)-n, otherwise a(n+1) = a(n)+n.
%H A392725 James C. McMahon, <a href="/A392725/b392725.txt">Table of n, a(n) for n = 1..10000</a>
%p A392725 R:= 1: x:= 1:
%p A392725 for n from 1 to 100 do
%p A392725   if convert(convert(abs(x),base,10),`+`)::odd then x:= x - n else x:= x + n fi;
%p A392725   R:= R,x
%p A392725 od:
%p A392725 R; # _Robert Israel_, Jan 21 2026
%t A392725 s={1};Do[If[OddQ[DigitSum[s[[-1]]]],AppendTo[s,s[[-1]]-n],AppendTo[s,s[[-1]]+n]],{n,1,56}];s (* _James C. McMahon_, Jan 26 2026 *)
%o A392725 (Python)
%o A392725 def digit_sum(x):
%o A392725     return sum(int(d) for d in str(abs(x)))
%o A392725 a = [1]  # a(1) = 1
%o A392725 for n in range(1, 100):
%o A392725     prev = a[-1]
%o A392725     if digit_sum(prev) % 2 == 1:  # odd
%o A392725         next_term = prev - n
%o A392725     else:  # even
%o A392725         next_term = prev + n
%o A392725     a.append(next_term)
%o A392725 print(a)
%o A392725 (PARI) lista(nn) = my(va = vector(nn)); va[1] = 1; for (n=2, nn, if (sumdigits(va[n-1]) % 2, va[n] = va[n-1] - (n-1), va[n] = va[n-1] + (n-1));); va; \\ _Michel Marcus_, Jan 21 2026
%Y A392725 Cf. A007953.
%K A392725 sign,easy
%O A392725 1,3
%A A392725 _Joshua B. Weinstein_, Jan 21 2026
