%I A392534 #13 Jan 16 2026 13:38:50
%S A392534 1,1,1,87,1436,28065,24152,3049073,388515788
%N A392534 a(n) is the least number k such that j * n! + 1 is prime for all j from k to k + n - 1.
%C A392534 Dickson's conjecture implies that a(n) exists for all n.
%C A392534 a(9), a(10) and a(11) all > 10^7.
%e A392534 a(4) = 87 because 87 * 4! + 1 = 2089, 88 * 4! + 1 = 2113, 89 * 4! + 1 = 2137, and 90 * 4! + 1 = 2161 are all prime, and there is no k < 87 such that k * 4! + 1, (k+1)*4! + 1, (k+2)*4! + 1 and (k+3)*4! + 1 are all prime.
%p A392534 f:= proc(n) local k,s;
%p A392534    s:= 0:
%p A392534    for k from 1 to 10^7 do
%p A392534      if isprime(k*n!+1) then s:= s+1; if s = n then return k-n+1 fi;
%p A392534      else s:= 0
%p A392534      fi
%p A392534   od;
%p A392534   FAIL
%p A392534 end proc:
%p A392534 map(f, [$1..8]);
%o A392534 (PARI) isok(k,n) = for(j=k,  k + n - 1, if (!ispseudoprime(j*n! + 1), return(0))); return(1);
%o A392534 a(n) = my(k=1); while (!isok(k, n), k++); k; \\ _Michel Marcus_, Jan 16 2026
%K A392534 nonn,more,new
%O A392534 1,4
%A A392534 _Robert Israel_, Jan 15 2026
%E A392534 a(9) from _Michel Marcus_, Jan 16 2026
