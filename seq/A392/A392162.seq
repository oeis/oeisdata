%I A392162 #25 Jan 12 2026 14:29:18
%S A392162 2,30,154,60,182,66,70,78,105,102,110,42,130,84,165,114,140,132,170,
%T A392162 126,190,138,195,168,220,156,230,174,231,90,238,120,266,150,273,180,
%U A392162 286,186,255,198,260,204,280,222,285,234,290,228,308,240
%N A392162 a(n) is the smallest number not yet in the sequence that has exactly one prime factor in common with a(n-1) and has at least two other different prime factors that are not factors of a(n-1); a(1) = 2.
%H A392162 Michael S. Branicky, <a href="/A392162/b392162.txt">Table of n, a(n) for n = 1..10000</a>
%e A392162 a(2) = 30 since it shares the prime 2 with a(1) = 2 and has two other different primes not contained in a(1) (3 and 5).
%e A392162 a(3) = 154 = 2*7*11 since it shares the prime 2 with a(2) = 30 and has two other different primes not contained in a(2) (7 and 11).
%o A392162 (Python)
%o A392162 from sympy import factorint
%o A392162 from functools import cache
%o A392162 from itertools import count, islice
%o A392162 @cache
%o A392162 def pf(n): return set(factorint(n))
%o A392162 def cond(pfan, pfk): return len(pfk&pfan) == 1 and len(pfk-pfan) >= 2
%o A392162 def agen():
%o A392162     an, aset, m = 2, {2}, 3
%o A392162     while True:
%o A392162         yield an
%o A392162         pfan = pf(an)
%o A392162         m = next(k for k in count(m) if k not in aset and len(pf(k)) >= 3)
%o A392162         an = next(k for k in count(m) if k not in aset and cond(pfan, pf(k)))
%o A392162         aset.add(an)
%o A392162 print(list(islice(agen(), 55))) # _Michael S. Branicky_, Jan 06 2026
%Y A392162 Cf. A064413, A350352.
%K A392162 nonn
%O A392162 1,1
%A A392162 _Enrique Navarrete_, Jan 01 2026
