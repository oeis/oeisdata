%I A392029 #13 Feb 28 2026 16:52:33
%S A392029 1,1,2,7,40,364,5100,107601
%N A392029 Number of sparse alternating sign matrices of size n.
%C A392029 An alternating sign matrix (ASM) is sparse if it cannot be turned into another ASM by replacing nonzero entries by zeros.
%e A392029 The first ASM is not sparse, because it can be turned into the second one by replacing nonzero entries by zeros:
%e A392029   [0 + 0 0]       [0 + 0 0]
%e A392029   [+ - + 0]       [+ 0 0 0]
%e A392029   [0 + - +]       [0 0 0 +]
%e A392029   [0 0 + 0]       [0 0 + 0]
%o A392029 (SageMath)
%o A392029 def is_sparse_ASM(M):
%o A392029     M = M.to_matrix()
%o A392029     n = M.nrows()
%o A392029     E = []
%o A392029     for i in range(1, n-1):
%o A392029         a1,b1 = 0, None
%o A392029         a2,b2 = 0, None
%o A392029         for j in range(1, n):
%o A392029             if M[i][j] == 1:
%o A392029                 a1 = j
%o A392029                 if b1:
%o A392029                     E.append(((i, a1), (i, b1)))
%o A392029             elif M[i][j] == -1:
%o A392029                 b1 = j
%o A392029                 E.append(((i, a1), (i, b1)))
%o A392029             if M[j][i] == 1:
%o A392029                 a2 = j
%o A392029                 if b2:
%o A392029                     E.append(((b2, i), (a2, i)))
%o A392029             elif M[j][i] == -1:
%o A392029                 b2 = j
%o A392029                 E.append(((b2, i), (a2, i)))
%o A392029     return DiGraph(E).is_directed_acyclic()
%o A392029 a = lambda n: sum(1 for M in AlternatingSignMatrices(n) if is_sparse_ASM(M))
%o A392029 print([a(k) for k in range(7)])
%Y A392029 Cf. A005130.
%K A392029 nonn,more,new
%O A392029 0,3
%A A392029 _Ludovic Schwob_, Feb 22 2026
