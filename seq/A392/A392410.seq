%I A392410 #17 Feb 28 2026 19:46:14
%S A392410 1,1,2,7,40,367,5476,133540
%N A392410 Number of full alternating sign matrices of size n.
%C A392410 An alternating sign matrix (ASM) if full if it cannot be turned into another ASM by replacing zero entries by nonzero entries.
%C A392410 Full ASMs correspond to maximal ASMs in the sense of Brualdi, Kiernan, Meyer and Schroeder (see reference in the links).
%C A392410 Permutation matrices which are full ASMs are the permutations avoiding patterns 2143 and 3412 (cf. A029759).
%H A392410 Richard A. Brualdi, Kathleen P. Kiernan, Seth A. Meyer, and Michael W. Schroeder, <a href="https://doi.org/10.1016/j.laa.2012.03.009">Patterns of alternating sign matrices</a>, Linear Algebra and its Applications 438 (2013), 3967-3990.
%e A392410 The first ASM is not full, because it can be turned into the second one by replacing zeros by nonzero entries:
%e A392410   [0 + 0 0]       [0 + 0 0]
%e A392410   [+ 0 0 0]       [+ - + 0]
%e A392410   [0 0 0 +]       [0 + - +]
%e A392410   [0 0 + 0]       [0 0 + 0]
%o A392410 (SageMath)
%o A392410 def is_full_ASM(M):
%o A392410     M = M.to_matrix()
%o A392410     n = M.nrows()
%o A392410     E = []
%o A392410     for i in range(n):
%o A392410         a0, t = 0, False
%o A392410         for j in range(n):
%o A392410             if M[i][j] != 0:
%o A392410                 for k in range(a0, j-1):
%o A392410                     if t:
%o A392410                         E.append(((i, k), (i, k+1)))
%o A392410                     else:
%o A392410                         E.append(((i, k+1),(i, k)))
%o A392410                 t = not t
%o A392410                 a0 = j+1
%o A392410         for k in range(a0, n-1):
%o A392410             if t:
%o A392410                 E.append(((i, k), (i, k+1)))
%o A392410             else:
%o A392410                 E.append(((i, k+1), (i, k)))
%o A392410         a0, t = 0, False
%o A392410         for j in range(n):
%o A392410             if M[j][i] != 0:
%o A392410                 for k in range(a0, j-1):
%o A392410                     if t:
%o A392410                         E.append(((k+1, i), (k, i)))
%o A392410                     else:
%o A392410                         E.append(((k, i), (k+1, i)))
%o A392410                 t = not t
%o A392410                 a0 = j+1
%o A392410         for k in range(a0, n-1):
%o A392410             if t:
%o A392410                 E.append(((k+1, i), (k, i)))
%o A392410             else:
%o A392410                 E.append(((k, i), (k+1, i)))
%o A392410     return DiGraph(E).is_directed_acyclic()
%o A392410 a = lambda n: sum(1 for M in AlternatingSignMatrices(n) if is_full_ASM(M))
%o A392410 print([a(k) for k in range(7)])
%Y A392410 Cf. A005130, A029759, A392029.
%K A392410 nonn,more,new
%O A392410 0,3
%A A392410 _Ludovic Schwob_, Feb 22 2026
