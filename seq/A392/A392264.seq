%I A392264 #35 Feb 14 2026 15:56:29
%S A392264 2,3,2,3,2,5,7,3,2,3,2,2,3,2,2,19,17,13,13,11,11,13,11,7,7,5,5,3,2,2,
%T A392264 3,2,2,3,2,2,37,41,37,31,71,29,31,29,29,61,29,23,43,23,23,19,23,17,19,
%U A392264 17,17,13,17,11,13,11,11,7,11,5,7,3,2,3,2,2,3,2,17,3,2,13,13,11,11,13,23,7,7
%N A392264 a(n) is the least prime p such that n + p is the digit reversal of a prime.
%C A392264 Trailing zeros in n + p are not allowed.
%H A392264 Robert Israel, <a href="/A392264/b392264.txt">Table of n, a(n) for n = 1..10000</a>
%e A392264 a(16) = 19 because 19 is a prime, the reversal of 16 + 19 = 35 is 53, which is prime, and no prime < 19 works.
%p A392264 rev:= proc(n) local L,i;
%p A392264   L:= convert(n,base,10);
%p A392264   add(L[-i]*10^(i-1),i=1..nops(L))
%p A392264 end proc:
%p A392264 f:= proc(n) local p,v;
%p A392264    p:= 1:
%p A392264    do
%p A392264      p:= nextprime(p);
%p A392264      v:= n+p;
%p A392264      if v mod 10 = 0 then next fi;
%p A392264      v:= rev(v);
%p A392264      if isprime(v) then return p fi
%p A392264    od
%p A392264 end proc:
%p A392264 map(f, [$1..100]);
%o A392264 (PARI) a(n) = my(p=2); while (!(((n+p)%10) && isprime(fromdigits(Vecrev(digits(n+p))))), p=nextprime(p+1)); p; \\ _Michel Marcus_, Feb 09 2026
%Y A392264 Cf. A393287.
%K A392264 nonn,base,new
%O A392264 1,1
%A A392264 _Robert Israel_, Feb 09 2026
