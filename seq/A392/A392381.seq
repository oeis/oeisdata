%I A392381 #5 Jan 29 2026 16:44:57
%S A392381 1,1,1,1,1,1,1,1,1,2,1,1,2,1,2,1,2,2,3,1,1,1,3,2,2,1,2,2,1,2,4,1,2,3,
%T A392381 2,3,1,4,1,4,1,3,3,5,2,1,4,1,1,1,1,4,1,2,2,5,2,4,2,4,3,3,1,1,2,7,1,1,
%U A392381 2,5,1,3,3,2,5,1,7,1,4,4,3,6,1,2,2,2,7,1
%N A392381 a(n) is the number of distinct nondegenerate triangles that can be formed by partitioning the multiset of prime factors of A392380(n) into three nonempty subsets whose sums are the triangle sides.
%H A392381 Felix Huber, <a href="/A392381/b392381.txt">Table of n, a(n) for n = 1..10000</a>
%e A392381 a(18) = 2 since A392380(18) = 90, whose multiset of prime factors {2, 3, 3, 5} can be partitioned into three nonempty subsets {2}, {5}, {3, 3} and {3}, {2, 3}, {5} whose sums form the two nondegenerate triangles (2, 5, 6) and (3, 5, 5).
%p A392381 b := proc(l)
%p A392381     local c, d, e, i, j, k, m, r, s, t, u, x;
%p A392381     m := sort(l, `>`); k := nops(m); t := add(m[i], i = 1 .. k); s := [[0, 0, 0]];
%p A392381     for i to k do
%p A392381         x := m[i]; u := [];
%p A392381         for j to nops(s) do
%p A392381             c := s[j][1]; d := s[j][2]; e := s[j][3];
%p A392381             if x + c < 1/2*t then u := [op(u), sort([x + c, d, e])]; end if;
%p A392381             if d + x < 1/2*t then u := [op(u), sort([c, d + x, e])]; end if;
%p A392381             if e + x < 1/2*t then u := [op(u), sort([c, d, e + x])]; end if;
%p A392381         end do;
%p A392381         s := convert({op(u)}, list);
%p A392381     end do;
%p A392381     r := 0;
%p A392381     for j to nops(s) do
%p A392381         c := s[j][1]; d := s[j][2]; e := s[j][3];
%p A392381         if 0 < c and 0 < d and 0 < e then r := r + 1; end if;
%p A392381     end do;
%p A392381     r;
%p A392381 end proc:
%p A392381 A392381 := proc(n)
%p A392381     local i, j;
%p A392381     b([seq(seq(i[1], j = 1 .. i[2]), i in ifactors(A392380(n))[2])]);
%p A392381 end proc:
%p A392381 seq(A392381(n), n = 1 .. 88);
%Y A392381 Cf. A001221, A001222, A001414, A006530, A030514, A033942, A306678, A366398, A376348, A378819, A379033, A389414, A392380.
%K A392381 nonn,easy
%O A392381 1,10
%A A392381 _Felix Huber_, Jan 24 2026
